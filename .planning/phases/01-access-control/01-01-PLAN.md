---
phase: 01-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/allowlist.py
  - src/auth/errors.py
  - src/auth/limiter.py
  - src/auth/service.py
  - src/sessions/store.py
  - src/models/auth.py
  - tests/test_auth.py
  - tests/conftest.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Valid personal code can be validated and bound to session authorization state."
    - "Invalid or missing code produces explicit unauthorized machine code and RU message."
  artifacts:
    - path: src/auth/service.py
      provides: "Access code validation and auth decision orchestration"
      min_lines: 40
    - path: src/sessions/store.py
      provides: "Session state with authorized flag lifecycle"
      min_lines: 30
    - path: tests/test_auth.py
      provides: "Automated checks for AUTH-01 and AUTH-02"
      min_lines: 40
  key_links:
    - from: src/auth/service.py
      to: src/auth/allowlist.py
      via: "allowlist parse + compare"
      pattern: "from src\\.auth\\.allowlist import"
    - from: src/auth/service.py
      to: src/auth/limiter.py
      via: "failed-attempt and cooldown decisions"
      pattern: "from src\\.auth\\.limiter import"
---

<objective>
Build the core access-control domain primitives (allowlist parser, validator, cooldown limiter, typed unauthorized errors, and session authorization state) as the foundation for endpoint integration.

Purpose: Establish deterministic and testable auth logic before wiring chat APIs.
Output: Reusable auth modules and first auth test suite skeleton.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-control/01-CONTEXT.md
@.planning/phases/01-access-control/01-RESEARCH.md
@.planning/phases/01-access-control/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement allowlist validation and unauthorized contract</name>
  <files>src/auth/allowlist.py, src/auth/errors.py, src/models/auth.py, src/auth/service.py</files>
  <read_first>.planning/phases/01-access-control/01-CONTEXT.md, .planning/phases/01-access-control/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create typed auth models and deterministic validation flow: parse AUTH_ALLOWLIST_CODES from env as comma-separated values, normalize with strip() on both env entries and user input, compare using hmac.compare_digest, and return explicit unauthorized payload with code values AUTH_INVALID_CODE or AUTH_MISSING_CODE plus RU message text. Keep exact case-sensitive code matching after trimming.</action>
  <verify>python -m pytest tests/test_auth.py::test_allowlist_parsing_and_validation -q</verify>
  <acceptance_criteria>
    - src/auth/allowlist.py contains "AUTH_ALLOWLIST_CODES" and "split(',')"
    - src/auth/service.py contains "hmac.compare_digest"
    - src/auth/errors.py defines AUTH_INVALID_CODE and AUTH_MISSING_CODE
    - src/models/auth.py defines a typed unauthorized response model with code and message fields
  </acceptance_criteria>
  <done>Allowlist parsing and auth decision logic are implemented with typed unauthorized responses.</done>
</task>

<task type="auto">
  <name>Task 2: Implement session auth state and cooldown limiter primitives</name>
  <files>src/sessions/store.py, src/auth/limiter.py, src/auth/service.py, tests/conftest.py, tests/test_auth.py</files>
  <read_first>.planning/phases/01-access-control/01-CONTEXT.md, .planning/phases/01-access-control/01-VALIDATION.md, src/auth/service.py</read_first>
  <action>Implement in-memory session store with session_id and authorized boolean, plus lifecycle methods to mark session authorized and invalidate on session end. Add per-IP (and optional bootstrap key) failed-attempt counters with default policy: 5 failures per 5 minutes triggers 10-minute cooldown; lockout returns AUTH_COOLDOWN with retry_after_seconds. Ensure log helper masks provided codes (example: AB***YZ) and never logs full value.</action>
  <verify>python -m pytest tests/test_auth.py::test_cooldown_and_masked_logging -q</verify>
  <acceptance_criteria>
    - src/sessions/store.py contains "authorized" session field and invalidation method
    - src/auth/limiter.py contains default constants for 5 failures, 300-second window, 600-second cooldown
    - src/auth/service.py handles AUTH_COOLDOWN before code comparison when lockout active
    - tests/test_auth.py includes assertions that raw access code is not present in auth failure logs
  </acceptance_criteria>
  <done>Session auth state and brute-force guardrail primitives exist and are test-covered.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest tests/test_auth.py -x -q
- [ ] Unauthorized payload contract is stable and machine-readable
- [ ] Session authorization state can be set and invalidated
</verification>

<success_criteria>
- All tasks completed
- AUTH-01 and AUTH-02 primitives implemented with automated tests
- Auth logic reusable by API handlers in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-control/01-01-SUMMARY.md`
</output>
