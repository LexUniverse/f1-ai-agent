---
phase: 01-access-control
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/api/chat.py
  - src/main.py
  - src/auth/dependencies.py
  - src/sessions/store.py
  - tests/test_auth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Authorized user can start a session once and continue within that session without re-entering code."
    - "Invalid or missing code is rejected before downstream chat processing."
    - "Unauthorized session access to protected message paths is blocked deterministically."
  artifacts:
    - path: src/api/chat.py
      provides: "Session bootstrap auth gate and protected endpoint guard usage"
      min_lines: 50
    - path: src/auth/dependencies.py
      provides: "Reusable pre-handler authorized-session dependency"
      min_lines: 20
    - path: tests/test_auth.py
      provides: "Integration tests proving pre-processing block behavior"
      min_lines: 80
  key_links:
    - from: src/api/chat.py
      to: src/auth/dependencies.py
      via: "Depends(require_authorized_session)"
      pattern: "Depends\\(require_authorized_session\\)"
    - from: src/api/chat.py
      to: src/auth/service.py
      via: "start_chat authorization check"
      pattern: "validate_access_code"
---

<objective>
Integrate access-control primitives into API entrypoints so unauthorized users are blocked before chat processing and authorized users receive session-bound access.

Purpose: Complete phase-level behavior for AUTH-01 and AUTH-02 with endpoint-level enforcement.
Output: Auth-enforced start/protected flows with integration tests that prove pre-processing blocks.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-control/01-CONTEXT.md
@.planning/phases/01-access-control/01-RESEARCH.md
@.planning/phases/01-access-control/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce auth on session bootstrap and bind session authorization</name>
  <files>src/api/chat.py, src/main.py, src/sessions/store.py, tests/test_auth.py</files>
  <read_first>src/auth/service.py, src/sessions/store.py, .planning/phases/01-access-control/01-CONTEXT.md, .planning/phases/01-access-control/01-RESEARCH.md</read_first>
  <action>Implement/start the chat bootstrap endpoint handler so it requires access_code, invokes validate_access_code(), creates session only when result is allowed, and stores authorized=true in session state for that session_id. Return explicit unauthorized responses for missing/invalid/cooldown states using stable machine codes and RU messages.</action>
  <verify>python -m pytest tests/test_auth.py::test_start_chat_authorization_flow -q</verify>
  <acceptance_criteria>
    - src/api/chat.py contains access_code validation in start-chat bootstrap flow
    - src/api/chat.py writes authorized session state only after successful validation
    - tests/test_auth.py asserts invalid and missing code paths return unauthorized codes
    - tests/test_auth.py asserts valid code creates authorized session
  </acceptance_criteria>
  <done>Session bootstrap endpoint is auth-gated and binds authorization to session state.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-handler guard for protected message paths</name>
  <files>src/auth/dependencies.py, src/api/chat.py, tests/test_auth.py</files>
  <read_first>src/api/chat.py, src/sessions/store.py, .planning/REQUIREMENTS.md, .planning/phases/01-access-control/01-RESEARCH.md</read_first>
  <action>Create require_authorized_session dependency that loads session by session_id, rejects missing/unauthorized sessions with AUTH_UNAUTHORIZED payload, and attach it to protected message/status handlers before downstream processing calls. Add integration test that spies or sentinel-checks chat pipeline function is NOT invoked when auth guard fails.</action>
  <verify>python -m pytest tests/test_auth.py::test_unauthorized_blocked_before_processing -q</verify>
  <acceptance_criteria>
    - src/auth/dependencies.py defines require_authorized_session
    - src/api/chat.py protected handlers include Depends(require_authorized_session)
    - tests/test_auth.py includes assertion that downstream processing is not called on unauthorized requests
    - unauthorized protected call returns AUTH_UNAUTHORIZED code
  </acceptance_criteria>
  <done>Protected endpoints are deterministically blocked before any chat processing for unauthorized sessions.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest tests/test_auth.py -x -q
- [ ] Unauthorized requests cannot reach downstream chat processing code
- [ ] Valid session can access protected message/status paths without re-entering code
</verification>

<success_criteria>
- All tasks completed
- AUTH-01 and AUTH-02 satisfied at endpoint behavior level
- Unauthorized block proven by integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-control/01-02-SUMMARY.md`
</output>
