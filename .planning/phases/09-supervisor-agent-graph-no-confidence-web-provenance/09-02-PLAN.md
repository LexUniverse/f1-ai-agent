---
phase: 09-supervisor-agent-graph-no-confidence-web-provenance
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01-PLAN.md"
files_modified:
  - src/models/api_contracts.py
  - src/api/chat.py
  - streamlit_app.py
  - tests/
autonomous: true
requirements:
  - API-05
  - WEB-01
  - SRCH-03
must_haves:
  truths:
    - "Sample NextMessageResponse-style JSON built by chat layer has no `details.confidence` and no top-level confidence field."
    - "When graph state includes web search usage, `details.web` is a dict with queries list and results list with url/title/snippet fields (WEB-01)."
    - "Streamlit does not reference `details['confidence']` or confidence UI (align Phase 10 UI-04/05 prep; minimal strip now)."
  artifacts:
    - path: "src/api/chat.py"
      provides: "assembles details without confidence; injects details.web"
    - path: "src/models/api_contracts.py"
      provides: "optional typed WebSearchDetails model for documentation/validation"
---

<objective>
**Phase 9 Wave 2:** Wire **`/next_message`** to **API-05** (no confidence anywhere in response JSON), **WEB-01** (`details.web` from graph state), and remove confidence display from **Streamlit**; add/extend **tests** proving contract. SRCH-03 integration: assert primary **`message`** is non-empty for happy path mocks (optional contract test).
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-supervisor-agent-graph-no-confidence-web-provenance/09-CONTEXT.md
@src/api/chat.py
@src/models/api_contracts.py
@streamlit_app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic models for details.web</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
  </read_first>
  <action>
    1. Add **`WebSearchResultItem`** (`url: str`, `title: str | None`, `content_snippet: str`) and **`WebSearchDetails`** (`queries: list[str]`, `results: list[WebSearchResultItem]`) to **`api_contracts.py`**.
    2. Document in docstrings that **`details.web`** in JSON responses follows this shape (serialized as dict via **`.model_dump()`** in chat layer).
  </action>
  <acceptance_criteria>
    - `rg "class WebSearchDetails" src/models/api_contracts.py` exits 0.
    - `rg "WebSearchResultItem" src/models/api_contracts.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.py — strip confidence, populate details.web</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
  </read_first>
  <action>
    1. Remove all logic that copies **`confidence`** from graph **`out_details`** into API **`details`**.
    2. If **`out_details`** contains **`web`** or **`tavily_queries` + `web_results`** (use the keys **actually** produced by **`f1_turn_graph`** after 09-01 — align field names in **09-01** if needed so chat can build **`WebSearchDetails`** deterministically), map them to **`details["web"] = WebSearchDetails(...).model_dump()`** when web was used; **omit** key when no web search occurred.
    3. Ensure **`structured_answer`** in **`details`** remains if present; **strip** any nested **`confidence`** if it existed inside structured payloads (grep **`confidence`** under **`src/api/chat.py`** → should be **0** matches after task).
    4. Grep **`src/api/chat.py`** for string **`confidence`** — must be **empty** result.
  </action>
  <acceptance_criteria>
    - `rg -i "confidence" /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py` exits 1.
    - `rg "details\\[\"web\"\\]|details\\.get\\(\"web\"" src/api/chat.py` exits 0 **OR** `rg '"web"' src/api/chat.py` shows assignment path for web details.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: streamlit_app.py — remove confidence UI</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/streamlit_app.py
  </read_first>
  <action>
    Remove **`_render_assistant_block`** branches that render **«Уверенность»** / **`details.get("confidence")`**. Keep **message**, **sources**, **synthesis** expander as appropriate for Phase 10 follow-up.
  </action>
  <acceptance_criteria>
    - `rg -i "confidence|Уверенность" /Users/alexshrestha/Documents/pet/AIAgent/streamlit_app.py` exits 1.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Contract tests API-05 + WEB-01</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/conftest.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
  </read_first>
  <action>
    1. Add **`tests/test_next_message_contract_phase9.py`** (or extend an existing chat test file) that builds **`details`** the same way **`chat.py`** does (import helper if refactored, or call a **thin pure function** extracted for testability — **optional** refactor only if needed).
    2. Assert JSON-serialized **`details`** for a **web** scenario contains **`"web"`** key with **`queries`** and **`results`** lists.
    3. Assert **`"confidence"`** not in **`details`** for both RAG-only and web scenarios (use **`json.dumps`** on dict).
    4. Run **`python -m pytest`** on the new file; exit 0.
  </action>
  <acceptance_criteria>
    - `test -f /Users/alexshrestha/Documents/pet/AIAgent/tests/test_next_message_contract_phase9.py` exits 0.
    - `python -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_next_message_contract_phase9.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Repo-wide confidence cleanup in response path</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/russian_qna.py
  </read_first>
  <action>
    1. **`rg "confidence" src/`** — remove or stop using **`QnAConfidence`** in any code path that builds **`NextMessageResponse`** / graph **`out_details`** for production. It is acceptable to leave **`QnAConfidence`** class in **`api_contracts.py`** unused **only if** `rg "QnAConfidence" src/` shows **no** usage outside **`api_contracts.py`**; otherwise delete class and fix imports.
    2. Update **`russian_qna.py`** / **`build_structured_ru_answer`** callers if they passed confidence into details — must not.
    3. Run **`python -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -q`** — all green (skip integration if marked).
  </action>
  <acceptance_criteria>
    - `rg "details\\[(\"|')confidence(\"|')\\]|\\[\"confidence\"\\]" /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py` exits 1.
    - `rg '"confidence"' /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py` exits 1.
    - `python -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -q --ignore-glob='*integration*'` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- Align **`f1_turn_graph`** `out_details` keys between **09-01** and **09-02** in the **same PR/commit series** if Wave 1 landed without `web` payload — **09-02 Task 2** may add a small follow-up edit to **`f1_turn_graph.py`** to emit **`tavily_queries`** + **`web_results`** for **`chat.py`** (acceptable if documented in commit message).
