---
phase: 09-supervisor-agent-graph-no-confidence-web-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/search/messages_ru.py
  - src/answer/gigachat_rag.py
  - src/graph/f1_turn_graph.py
  - tests/test_f1_supervisor_graph.py
autonomous: true
requirements:
  - AGT-03
  - AGT-04
  - AGT-05
  - SRCH-03
must_haves:
  truths:
    - "LangGraph run includes an explicit supervisor decision on full candidate answer text vs user_question (AGT-03)."
    - "First synthesis path uses only RAG evidence; Tavily invoked only after supervisor reject; at most 2 Tavily invocations per turn after that RAG attempt (AGT-04)."
    - "After 2 web rounds and supervisor still rejects, out_message equals UNABLE_TO_ANSWER_SUPERVISOR_RU from messages_ru (AGT-05)."
    - "GigachatRUSynthesisResult and graph out_details contain no confidence field (API-05 partial — chat wiring in 09-02)."
    - "Accepted web-path message answers original question per SRCH-03 prompts (enforced by supervisor + worker prompts)."
  artifacts:
    - path: "src/graph/f1_turn_graph.py"
      provides: "supervisor loop, web_search_rounds cap, terminal failure path"
    - path: "src/answer/gigachat_rag.py"
      provides: "supervisor JSON judge, agent1_rag-only, agent1_web synthesis without confidence in result type"
    - path: "tests/test_f1_supervisor_graph.py"
      provides: "mocked multi-step supervisor scenarios"
---

<objective>
Implement **Phase 9 Wave 1**: **supervisor–Agent 1 LangGraph** (AGT-03–05), **≤2 Tavily calls** after RAG-only attempt, **AGT-05** terminal string, **SRCH-03** via tightened worker/supervisor prompts, and remove **confidence** from **`GigachatRUSynthesisResult`** / graph **`out_details`** (remaining HTTP layer in **09-02**).
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-supervisor-agent-graph-no-confidence-web-provenance/09-CONTEXT.md
@.planning/phases/09-supervisor-agent-graph-no-confidence-web-provenance/09-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AGT-05 message constant</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/search/messages_ru.py
  </read_first>
  <action>
    Add module-level constant **`UNABLE_TO_ANSWER_SUPERVISOR_RU`** with exact value:

    **`Не удалось найти достаточно надёжного ответа на ваш вопрос по доступным данным.`**

    (Single line; no trailing newline change to unrelated constants.)
  </action>
  <acceptance_criteria>
    - `rg "UNABLE_TO_ANSWER_SUPERVISOR_RU" src/search/messages_ru.py` exits 0.
    - `rg "Не удалось найти достаточно надёжного ответа" src/search/messages_ru.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gigachat_rag — supervisor + workers, drop confidence from result type</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/09-supervisor-agent-graph-no-confidence-web-provenance/09-CONTEXT.md
  </read_first>
  <action>
    1. Change **`GigachatRUSynthesisResult`** to a **dataclass** (or NamedTuple) with fields **`message: str`** and **`structured_answer: StructuredRUAnswer`** only — **remove `confidence`**.
    2. Add **`gigachat_supervisor_accept_answer(*, user_question: str, candidate_answer: str) -> bool`** using **`_chat_completion_json`** with a **new** system prompt that requires JSON **`{"accept": true}`** or **`{"accept": false}`** (add Pydantic model **`_SupervisorAcceptPayload`**). On parse failure, return **`False`** (conservative).
    3. Update **`gigachat_synthesize_historical`** and **`gigachat_synthesize_from_web_results`** to **not** compute or return confidence; build **`StructuredRUAnswer`** as today for sources/sections.
    4. Strengthen **`_SYSTEM_WEB`** (and RAG system string if needed) with explicit bullets: answer the **original user question** in Russian; **do not** repeat only the user question; **do not** output search-query text as the sole answer; **do not** use only a source list without a factual answer sentence.
    5. Fix **all call sites** in the repo that referenced **`.confidence`** on **`GigachatRUSynthesisResult`** (grep-driven).
  </action>
  <acceptance_criteria>
    - `rg "class GigachatRUSynthesisResult" -A5 src/answer/gigachat_rag.py` shows no `confidence` field.
    - `rg "def gigachat_supervisor_accept_answer" src/answer/gigachat_rag.py` exits 0.
    - `rg "confidence" src/answer/gigachat_rag.py` exits 1 **OR** only appears in comments/strings not related to result type (goal: no `result.confidence`).
    - `python -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -q --ignore=tests/integration 2>/dev/null | tail -1` — if collection fails due to API change, fix tests in this task or note dependency on Task 3; prefer green unit tests before Task 3 completes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: f1_turn_graph — supervisor loop and Tavily cap</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/tavily_tool.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/search/messages_ru.py
  </read_first>
  <action>
    1. **Replace** the linear retrieve → sufficiency → tavily → synthesize flow with a graph that implements:
       - Node **`retrieve`** (unchanged retrieval + evidence).
       - Node **`agent1_rag`**: call **`gigachat_synthesize_historical`** with current evidence and **`user_question`**; store **`candidate_message`**, **`candidate_structured`**, **`synthesis_route`** (`gigachat_rag` or fallback).
       - Node **`supervisor`**: call **`gigachat_supervisor_accept_answer(user_question=..., candidate_answer=candidate_message)`**; set **`supervisor_accept: bool`**.
       - Conditional: if accept → **`finalize`** node that writes **`out_message`**, **`out_details`** (**no `confidence` key**; include **`structured_answer`**, **`synthesis`** route metadata).
       - If not accept and **`web_search_rounds < 2`**: node **`tavily_search`** calls **`run_tavily_search_once`** with query from **`gigachat_author_tavily_query(user_question=...)`** (reuse existing helper), append/increment **`web_search_rounds`**, merge **`web_results`** for **`details.web` assembly later** (store raw list on state).
       - After Tavily: node **`agent1_web`** calls **`gigachat_synthesize_from_web_results`**; update candidate; loop back to **`supervisor`**.
       - If not accept and **`web_search_rounds >= 2`** (i.e. already completed 2 Tavily invocations in this turn): route to **`finalize_fail`** setting **`out_message = UNABLE_TO_ANSWER_SUPERVISOR_RU`**, **`terminal_status=ready`**, structured minimal **`details`** with **`synthesis.route = "supervisor_gave_up"`** (or agreed constant).
    2. State **`TypedDict`** must include: **`web_search_rounds: int`** (initialize **0** before first Tavily), **`candidate_message: str`**, **`supervisor_accept: bool`**, **`web_results: list[dict]`** (latest or accumulated per design — document in code comment), **`tavily_queries: list[str]`** for WEB-01 in 09-02.
    3. **Remove** any **`confidence`** assignment from **`out_details`** in this file.
    4. Preserve **`WEB_SEARCH_UNAVAILABLE`** behavior when Tavily errors (existing degraded RU message path) — count as a search round only if invocation was attempted per your mapping; document in comment.
    5. Export **`run_f1_turn_sync`** with **`recursion_limit`** high enough for the loop (e.g. **25**).
  </action>
  <acceptance_criteria>
    - `rg "web_search_rounds" src/graph/f1_turn_graph.py` exits 0.
    - `rg "gigachat_supervisor_accept_answer" src/graph/f1_turn_graph.py` exits 0.
    - `rg "\"confidence\"" src/graph/f1_turn_graph.py` exits 1 (no confidence string key in dict literals — use rg carefully: `confidence` key omitted from out_details).
    - `rg "confidence" src/graph/f1_turn_graph.py` — if matches, only in imports to be deleted: remove **`live_qna_confidence`**, **`qna_confidence_from_evidence`** from graph if unused.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tests/test_f1_supervisor_graph.py</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_f1_assistant_graph.py
  </read_first>
  <action>
    Add **`tests/test_f1_supervisor_graph.py`** with **pytest** tests using **`monkeypatch`**:
    1. **`test_supervisor_accepts_rag_skips_tavily`**: mock supervisor **True** after first RAG; mock **`run_tavily_search_once`** to **`pytest.fail`** if called; assert final **`out_message`** from mocked RAG synthesis.
    2. **`test_two_tavily_then_agt05`**: mock supervisor **False** for first three candidates, **True** never; mock Tavily returns minimal list; assert **`web_search_rounds`** ends at **2** and final **`out_message`** equals **`UNABLE_TO_ANSWER_SUPERVISOR_RU`** (import from **`src.search.messages_ru`**).
    3. **`test_supervisor_accepts_after_one_web`**: reject RAG, accept after web — assert **`run_tavily_search_once` called exactly once**.
    All tests **offline** (no real GigaChat/Tavily).
  </action>
  <acceptance_criteria>
    - `python -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_f1_supervisor_graph.py -q` exits 0.
    - `rg "test_two_tavily_then_agt05" tests/test_f1_supervisor_graph.py` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- **LangChain “tool”:** RESEARCH recommends **manual Tavily node** for cap enforcement; do **not** introduce an autonomous ReAct agent unless caps remain provable in code.
- If **`tests/test_f1_assistant_graph.py`** breaks due to graph shape change, **update or replace** tests in this plan wave so **`pytest tests/`** stays green.
