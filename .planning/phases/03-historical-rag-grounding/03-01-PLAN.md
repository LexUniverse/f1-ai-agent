---
phase: 03-historical-rag-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval/alias_resolver.py
  - src/retrieval/retriever.py
  - src/retrieval/evidence.py
  - src/models/api_contracts.py
  - tests/test_alias_resolution.py
autonomous: true
requirements:
  - RAG-01
  - RAG-02
must_haves:
  truths:
    - "User historical query is transformed into canonical RU/EN-aware retrieval input."
    - "Retriever returns deterministic top-k historical hits from f1db-indexed Chroma data."
    - "Evidence items have stable trace fields for downstream synthesis."
  artifacts:
    - path: "src/retrieval/alias_resolver.py"
      provides: "Dictionary-first RU/EN alias normalization and canonical entity mapping"
    - path: "src/retrieval/retriever.py"
      provides: "Deterministic Chroma retrieval API with top_k and min_score controls"
    - path: "src/retrieval/evidence.py"
      provides: "Evidence formatter producing source_id/snippet/entity_tags/rank_score"
    - path: "src/models/api_contracts.py"
      provides: "Typed evidence models for API contract usage"
    - path: "tests/test_alias_resolution.py"
      provides: "Automated RU/EN alias parity checks"
  key_links:
    - from: "src/retrieval/alias_resolver.py"
      to: "src/retrieval/retriever.py"
      via: "canonical entity IDs and normalized query text"
      pattern: "resolve\\(.*\\) -> .*canonical.*"
    - from: "src/retrieval/retriever.py"
      to: "src/retrieval/evidence.py"
      via: "retrieval hit dicts"
      pattern: "format_evidence\\(hits"
---

<objective>
Build the retrieval foundation for historical grounding in Phase 3 with explicit RU/EN alias resolution and deterministic evidence formatting.

Purpose: Satisfy dictionary-first multilingual matching and deterministic retrieval primitives before endpoint integration.
Output: New retrieval modules, typed evidence contract models, and alias-focused automated tests.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-rag-grounding/03-CONTEXT.md
@.planning/phases/03-historical-rag-grounding/03-RESEARCH.md
@src/models/api_contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define retrieval contracts and deterministic defaults</name>
  <read_first>
    - src/models/api_contracts.py
    - .planning/phases/03-historical-rag-grounding/03-CONTEXT.md
    - .planning/phases/03-historical-rag-grounding/03-RESEARCH.md
  </read_first>
  <files>src/models/api_contracts.py, src/retrieval/retriever.py, src/retrieval/evidence.py</files>
  <behavior>
    - Evidence item carries source_id, snippet, entity_tags, rank_score (per D-05).
    - Retriever exposes deterministic top_k and min_score arguments with defaults.
    - Retriever output shape is explicit and formatter-compatible.
  </behavior>
  <action>
    Add `EvidenceItem` and `RetrievalArtifacts` Pydantic models in `src/models/api_contracts.py` with exact fields:
    `source_id: str`, `snippet: str`, `entity_tags: list[str]`, `rank_score: float`, and `used_in_answer: bool = False`.
    Create `src/retrieval/retriever.py` with `DEFAULT_TOP_K = 5`, `DEFAULT_MIN_SCORE = 0.35`, and function signature
    `retrieve_historical_context(query: str, canonical_entity_ids: list[str], *, top_k: int = DEFAULT_TOP_K, min_score: float = DEFAULT_MIN_SCORE) -> list[dict]`.
    Implement explicit non-live scope guard (per D-07, D-08): source metadata filter must include `dataset="f1db"` and no live API calls.
    Create `src/retrieval/evidence.py` with `format_evidence(hits: list[dict], entity_tags: list[str]) -> list[EvidenceItem]` that truncates snippets to 280 chars.
  </action>
  <acceptance_criteria>
    - `rg "class EvidenceItem|class RetrievalArtifacts" src/models/api_contracts.py` returns both models.
    - `rg "DEFAULT_TOP_K = 5|DEFAULT_MIN_SCORE = 0.35|dataset=\"f1db\"" src/retrieval/retriever.py` returns all constants/filters.
    - `rg "def format_evidence\\(hits: list\\[dict\\], entity_tags: list\\[str\\]\\) -> list\\[EvidenceItem\\]" src/retrieval/evidence.py` returns formatter signature.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_alias_resolution.py -q</automated>
  </verify>
  <done>Retrieval and evidence contracts exist with exact required fields and deterministic defaults aligned to D-05, D-07, D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dictionary-first RU/EN alias resolver</name>
  <read_first>
    - .planning/phases/03-historical-rag-grounding/03-CONTEXT.md
    - src/api/chat.py
    - src/models/api_contracts.py
  </read_first>
  <files>src/retrieval/alias_resolver.py, tests/test_alias_resolution.py</files>
  <behavior>
    - RU and EN aliases map to canonical IDs for drivers/teams/races.
    - Normalization handles Unicode case/spacing/punctuation without fuzzy-first matching.
    - Unknown aliases do not silently map to random entities.
  </behavior>
  <action>
    Create `src/retrieval/alias_resolver.py` with `resolve_entities(query: str) -> tuple[str, list[str], list[str]]` returning
    `(normalized_query, canonical_entity_ids, entity_tags)`. Implement a canonical dictionary with at least these aliases:
    `["макс ферстаппен", "max verstappen"] -> "driver:max_verstappen"`,
    `["феррари", "scuderia ferrari", "ferrari"] -> "team:ferrari"`,
    `["гран-при монако", "monaco grand prix"] -> "race:monaco_gp"`.
    Enforce D-03 and D-04 by using exact normalized dictionary matching only (no difflib/fuzzy match in decision path).
    Add `tests/test_alias_resolution.py` with parameterized cases for RU/EN parity and unmatched input behavior.
  </action>
  <acceptance_criteria>
    - `rg "driver:max_verstappen|team:ferrari|race:monaco_gp" src/retrieval/alias_resolver.py` returns canonical IDs.
    - `rg "pytest\\.mark\\.parametrize|test_ru_en_aliases_map_to_same_canonical_ids|test_unknown_alias_returns_empty_entities" tests/test_alias_resolution.py` returns all tests.
    - `pytest tests/test_alias_resolution.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_alias_resolution.py -q</automated>
  </verify>
  <done>Alias resolver deterministically maps RU/EN aliases to canonical IDs and passes automated parity tests for RAG-02.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_alias_resolution.py -q` and ensure no live API interaction paths are introduced in retrieval modules.
</verification>

<success_criteria>
RAG foundation exists with deterministic retrieval primitives, dictionary-first alias resolution, and typed evidence schema ready for endpoint integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-rag-grounding/03-01-SUMMARY.md`
</output>
