---
phase: 03-historical-rag-grounding
plan: 04
type: execute
wave: 2
depends_on:
  - 03-03
files_modified:
  - src/retrieval/retriever.py
  - src/api/chat.py
  - tests/test_rag_grounding.py
  - tests/test_api_async_contracts.py
autonomous: true
requirements:
  - RAG-01
  - RAG-03
gap_closure: true
must_haves:
  truths:
    - "Runtime `/next_message` retrieval returns real ranked hits from indexed f1db chunks."
    - "Responses include evidence references tied to synthesis without monkeypatched retrieval."
    - "No simulated retrieval branch is used in normal runtime path."
  artifacts:
    - path: "src/retrieval/retriever.py"
      provides: "Real Chroma query implementation with top-k and score threshold filtering"
    - path: "src/api/chat.py"
      provides: "Inline retrieval that uses indexed corpus and preserves deterministic failure envelope"
    - path: "tests/test_rag_grounding.py"
      provides: "Integration checks using indexed corpus path instead of simulated hits"
    - path: "tests/test_api_async_contracts.py"
      provides: "Contract-level regression checks for evidence-bearing next_message responses"
  key_links:
    - from: "src/retrieval/retriever.py"
      to: "Chroma collection populated by 03-03"
      via: "_query_historical_index() query call"
      pattern: "collection\\.query|where=\\{.*dataset"
    - from: "src/api/chat.py"
      to: "src/retrieval/retriever.py"
      via: "retrieve_historical_context(normalized_query, canonical_entity_ids)"
      pattern: "retrieve_historical_context\\("
    - from: "src/api/chat.py"
      to: "NextMessageResponse.details.evidence"
      via: "formatted evidence model_dump payload"
      pattern: "details.*evidence"
---

<objective>
Close the runtime grounding gap by replacing stubbed retrieval with real Chroma queries over indexed `f1db-csv` chunks and validating end-to-end traceability.

Purpose: Turn existing wiring into production-valid behavior so RAG-01 and RAG-03 are observable without mocks.
Output: Real retriever query path, endpoint integration updates, and non-mocked gap-closing tests.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-historical-rag-grounding/03-VERIFICATION.md
@.planning/phases/03-historical-rag-grounding/03-03-PLAN.md
@src/retrieval/retriever.py
@src/api/chat.py
@tests/test_rag_grounding.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace simulated retrieval with real indexed-chunk Chroma query</name>
  <read_first>
    - src/retrieval/retriever.py
    - src/retrieval/index_builder.py
    - .planning/phases/03-historical-rag-grounding/03-VERIFICATION.md
  </read_first>
  <files>src/retrieval/retriever.py, tests/test_rag_grounding.py</files>
  <behavior>
    - `_query_historical_index` queries Chroma collection populated from `f1db-csv/`.
    - Results are mapped into stable `source_id/snippet/score` hit dicts and filtered by `min_score`.
    - Retrieval stays historical-only with `dataset=f1db` and canonical entity metadata filters.
  </behavior>
  <action>
    Replace `simulated_hits` implementation in `src/retrieval/retriever.py` with concrete Chroma client lookup:
    initialize client/collection (`f1_historical`), call `collection.query(query_texts=[query], n_results=top_k, where=...)`,
    flatten returned documents/metadatas/distances into ranked hits, convert distance/similarity into deterministic `score`, and apply `min_score`.
    Keep explicit historical constraint per D-07 (`dataset="f1db"` in filter) and use canonical filter when `canonical_entity_ids` are provided.
    Add/adjust test(s) in `tests/test_rag_grounding.py` to exercise retriever with a seeded tiny collection (no monkeypatch on retriever internals).
  </action>
  <acceptance_criteria>
    - `rg "collection\\.query|dataset.*f1db|canonical_entity_id|simulated_hits" src/retrieval/retriever.py` shows query/filter logic and no active simulated runtime branch.
    - `pytest tests/test_rag_grounding.py::test_retriever_returns_ranked_hits_from_seeded_index -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_grounding.py::test_retriever_returns_ranked_hits_from_seeded_index -q</automated>
  </verify>
  <done>Retriever returns non-empty ranked hits from indexed chunks in normal runtime path, removing the blocker identified in verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate endpoint grounding and evidence linkage against real retrieval path</name>
  <read_first>
    - src/api/chat.py
    - src/retrieval/retriever.py
    - tests/test_api_async_contracts.py
  </read_first>
  <files>src/api/chat.py, tests/test_rag_grounding.py, tests/test_api_async_contracts.py</files>
  <behavior>
    - `/next_message` returns `ready` with evidence when indexed data has relevant chunks.
    - `details.evidence` entries contain required traceability fields and `used_in_answer=true`.
    - Failure branch remains deterministic only for true no-evidence/error conditions.
  </behavior>
  <action>
    Update `src/api/chat.py` only as needed to consume real retriever output (no simulated fallback path) while keeping existing deterministic envelope semantics.
    Extend `tests/test_rag_grounding.py` and `tests/test_api_async_contracts.py` to run an end-to-end call that uses a seeded test index from `f1db-csv`-derived rows (or minimal deterministic fixture using the same schema as 03-03).
    Assert response `details.evidence` is non-empty for known historical query and includes `source_id`, `snippet`, `entity_tags`, `rank_score`, `used_in_answer`.
  </action>
  <acceptance_criteria>
    - `rg "test_next_message_grounds_from_indexed_f1db_context|test_response_contains_traceable_evidence_without_monkeypatch" tests/test_rag_grounding.py tests/test_api_async_contracts.py` returns both tests.
    - `pytest tests/test_rag_grounding.py tests/test_api_async_contracts.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_grounding.py tests/test_api_async_contracts.py -q</automated>
  </verify>
  <done>Endpoint-level behavior proves grounded answers and traceability from real indexed retrieval, closing RAG-01/RAG-03 verification gaps.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_rag_grounding.py tests/test_api_async_contracts.py -q` and confirm at least one non-mocked `/next_message` scenario returns `ready` with populated evidence from indexed corpus.
</verification>

<success_criteria>
Phase 3 runtime retrieval no longer depends on simulated hits: `/next_message` grounds answers on Chroma-indexed `f1db-csv` chunks with traceable evidence payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-rag-grounding/03-04-SUMMARY.md`
</output>
