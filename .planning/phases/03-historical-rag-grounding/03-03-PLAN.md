---
phase: 03-historical-rag-grounding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval/index_builder.py
  - src/retrieval/document_schema.py
  - src/retrieval/alias_resolver.py
  - tests/test_rag_indexing.py
autonomous: true
requirements:
  - RAG-01
  - RAG-02
gap_closure: true
must_haves:
  truths:
    - "Historical f1db CSV data is ingested into ChromaDB as deterministic chunks."
    - "Indexed documents include canonical entity metadata used for RU/EN alias-aware retrieval."
    - "Indexing can be rerun without duplicate logical records."
  artifacts:
    - path: "src/retrieval/document_schema.py"
      provides: "Deterministic document/chunk ID and metadata schema for f1db rows"
    - path: "src/retrieval/index_builder.py"
      provides: "CSV-to-Chroma embedding/upsert flow for f1db-csv dataset"
    - path: "tests/test_rag_indexing.py"
      provides: "Automated proof of deterministic IDs and idempotent upsert behavior"
  key_links:
    - from: "f1db-csv/*.csv"
      to: "src/retrieval/index_builder.py"
      via: "CSV reader -> normalized document schema"
      pattern: "csv\\.DictReader|build_document_from_row"
    - from: "src/retrieval/document_schema.py"
      to: "src/retrieval/index_builder.py"
      via: "deterministic chunk_id + metadata contract"
      pattern: "chunk_id|canonical_entity_id|dataset"
---

<objective>
Close the verification gap by replacing simulated retrieval preconditions with a real historical index built from `f1db-csv/`.

Purpose: Ensure runtime retrieval has an actual Chroma-backed corpus from authoritative project data (per D-07 and user constraint).
Output: Deterministic indexing pipeline, canonical metadata schema, and automated indexing validation tests.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-historical-rag-grounding/03-CONTEXT.md
@.planning/phases/03-historical-rag-grounding/03-RESEARCH.md
@.planning/phases/03-historical-rag-grounding/03-VERIFICATION.md
@src/retrieval/retriever.py
@f1db-csv/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic f1db document schema and canonical metadata</name>
  <read_first>
    - .planning/phases/03-historical-rag-grounding/03-CONTEXT.md
    - .planning/phases/03-historical-rag-grounding/03-VERIFICATION.md
    - src/retrieval/alias_resolver.py
  </read_first>
  <files>src/retrieval/document_schema.py, src/retrieval/alias_resolver.py, tests/test_rag_indexing.py</files>
  <behavior>
    - Chunk IDs are deterministic from dataset/table/row identity.
    - Metadata always includes `dataset`, `table`, `source_id`, and optional `canonical_entity_id`.
    - Canonical IDs align with dictionary-first resolver output for RU/EN aliases (per D-03, D-04).
  </behavior>
  <action>
    Create `src/retrieval/document_schema.py` defining a deterministic schema builder for CSV rows from `f1db-csv/`:
    `build_document_from_row(table_name: str, row: dict[str, str]) -> dict` returning `chunk_id`, `document_text`, and `metadata`.
    `chunk_id` must be stable across runs (hash of `dataset=f1db`, table, and stable row key fields).
    Add `canonical_entity_id` derivation hooks for drivers/teams/races aligned to resolver canonical IDs (per D-03 and RAG-02).
    Extend alias dictionary seed data only if required to map IDs used by indexing metadata.
    Add tests in `tests/test_rag_indexing.py` validating deterministic `chunk_id` generation and metadata contract on representative rows from drivers/races tables.
  </action>
  <acceptance_criteria>
    - `rg "def build_document_from_row|chunk_id|canonical_entity_id|dataset" src/retrieval/document_schema.py` returns all expected schema elements.
    - `rg "test_document_schema_is_deterministic|test_document_schema_contains_required_metadata" tests/test_rag_indexing.py` returns both tests.
    - `pytest tests/test_rag_indexing.py::test_document_schema_is_deterministic -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_indexing.py::test_document_schema_is_deterministic -q</automated>
  </verify>
  <done>Deterministic schema exists and maps f1db rows to stable chunk IDs with canonical metadata needed by retrieval filters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CSV ingestion and Chroma embedding/upsert pipeline</name>
  <read_first>
    - src/retrieval/document_schema.py
    - src/retrieval/retriever.py
    - .planning/phases/03-historical-rag-grounding/03-RESEARCH.md
  </read_first>
  <files>src/retrieval/index_builder.py, tests/test_rag_indexing.py</files>
  <behavior>
    - Pipeline reads CSV files from `f1db-csv/` only.
    - Indexing writes to a Chroma collection scoped to historical f1db dataset.
    - Re-running ingestion is idempotent via upsert with deterministic IDs.
  </behavior>
  <action>
    Create `src/retrieval/index_builder.py` with `build_historical_index(csv_root: str = "f1db-csv", collection_name: str = "f1_historical") -> dict`.
    Implement concrete flow: enumerate selected CSV files (drivers, constructors, races, race-results at minimum), parse rows, convert via `build_document_from_row`, batch embed text with Chroma collection API, and `upsert` IDs/documents/metadatas.
    Enforce user constraint directly: runtime path must depend on this indexed collection and must not fallback to simulated retrieval.
    Add tests proving idempotency (`build_historical_index` twice keeps unique logical IDs) and non-empty upsert from fixture/sample CSV rows.
  </action>
  <acceptance_criteria>
    - `rg "def build_historical_index|upsert|f1db-csv|f1_historical" src/retrieval/index_builder.py` returns expected pipeline symbols.
    - `rg "test_build_historical_index_upserts_from_csv|test_build_historical_index_is_idempotent" tests/test_rag_indexing.py` returns both tests.
    - `pytest tests/test_rag_indexing.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_indexing.py -q</automated>
  </verify>
  <done>Chroma ingestion pipeline populates historical collection from `f1db-csv/` with deterministic upsert behavior and passing tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_rag_indexing.py -q` and verify indexing completes with deterministic IDs and non-empty document writes from `f1db-csv/`.
</verification>

<success_criteria>
Historical f1db corpus is concretely indexed in ChromaDB from CSV sources, providing deterministic and alias-aware metadata required for grounded retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-rag-grounding/03-03-SUMMARY.md`
</output>
