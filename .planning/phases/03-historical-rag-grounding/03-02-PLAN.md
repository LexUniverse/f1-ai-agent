---
phase: 03-historical-rag-grounding
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/api/chat.py
  - src/sessions/store.py
  - tests/test_rag_grounding.py
autonomous: true
requirements:
  - RAG-01
  - RAG-03
must_haves:
  truths:
    - "User historical answer is synthesized from retrieved evidence produced before answer generation."
    - "User response includes traceable evidence references tied to the final synthesized output."
    - "Retrieval failure produces deterministic failed status/details without hidden processing."
  artifacts:
    - path: "src/api/chat.py"
      provides: "Inline retrieve-then-synthesize flow in /next_message"
    - path: "src/sessions/store.py"
      provides: "Deterministic retrieval-related session status transitions"
    - path: "tests/test_rag_grounding.py"
      provides: "Automated grounding and evidence linkage coverage"
  key_links:
    - from: "src/api/chat.py"
      to: "src/retrieval/alias_resolver.py"
      via: "resolve_entities() call before retrieval"
      pattern: "resolve_entities\\("
    - from: "src/api/chat.py"
      to: "src/retrieval/retriever.py"
      via: "retrieve_historical_context()"
      pattern: "retrieve_historical_context\\("
    - from: "src/api/chat.py"
      to: "NextMessageResponse.details.evidence"
      via: "typed response details payload"
      pattern: "details=\\{.*evidence"
---

<objective>
Integrate retrieval into `/next_message` so historical answers are evidence-grounded and traceable through the response contract.

Purpose: Satisfy end-to-end grounding and evidence-to-answer linkage in production API flow.
Output: Inline retrieval pipeline in endpoint, deterministic failure handling, and RAG grounding tests.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-rag-grounding/03-CONTEXT.md
@.planning/phases/03-historical-rag-grounding/03-RESEARCH.md
@.planning/phases/02-async-backend-contracts/02-02-SUMMARY.md
@src/api/chat.py
@src/sessions/store.py
@tests/test_api_async_contracts.py

<interfaces>
From src/api/chat.py:
`@router.post("/next_message", response_model=NextMessageResponse)`
`def next_message(_session=Depends(_session_dependency)): ...`

From src/models/api_contracts.py:
`class NextMessageResponse(BaseModel):`
`  message: str`
`  status: Literal["ready", "failed"]`
`  details: dict = Field(default_factory=dict)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire inline retrieve-then-synthesize flow in /next_message</name>
  <read_first>
    - src/api/chat.py
    - src/retrieval/alias_resolver.py
    - src/retrieval/retriever.py
    - src/retrieval/evidence.py
    - .planning/phases/03-historical-rag-grounding/03-CONTEXT.md
  </read_first>
  <files>src/api/chat.py, src/sessions/store.py</files>
  <behavior>
    - Retrieval executes inline in `/next_message` (D-01).
    - Retrieval artifacts are generated before answer synthesis (D-02).
    - Evidence is attached to response details and explicitly marked as used in synthesis (D-06).
  </behavior>
  <action>
    Update `next_message` in `src/api/chat.py` to execute this exact sequence:
    1) call `resolve_entities(_session.next_message)` to get normalized query + tags,
    2) call `retrieve_historical_context(normalized_query, canonical_entity_ids, top_k=5, min_score=0.35)`,
    3) call `format_evidence(hits, entity_tags)` and set `used_in_answer=True` for items consumed by synthesis,
    4) build deterministic synthesized message template: `"Историческая сводка: {top_snippet}"` when evidence exists, else `"Недостаточно исторических данных в базе f1db."`.
    Keep `status="failed"` branch deterministic with `details={"code": "...", "evidence": []}` and update store status via `SessionStore.set_status(...)`.
    Do not add queue/background workers and do not call live APIs (D-01, D-08).
  </action>
  <acceptance_criteria>
    - `rg "resolve_entities\\(|retrieve_historical_context\\(|format_evidence\\(" src/api/chat.py` returns all pipeline calls.
    - `rg "Историческая сводка|Недостаточно исторических данных в базе f1db" src/api/chat.py` returns both deterministic messages.
    - `rg "set_status\\(" src/api/chat.py src/sessions/store.py` confirms status transition usage exists.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py -q</automated>
  </verify>
  <done>`/next_message` performs inline retrieval before synthesis and returns deterministic evidence-bearing details for success and failure branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end grounding and traceability tests</name>
  <read_first>
    - tests/test_api_async_contracts.py
    - src/api/chat.py
    - src/models/api_contracts.py
    - .planning/phases/03-historical-rag-grounding/03-VALIDATION.md
  </read_first>
  <files>tests/test_rag_grounding.py</files>
  <behavior>
    - Historical answer uses retrieved evidence payload.
    - Response details include traceable evidence fields from D-05.
    - Evidence used for synthesis is explicitly represented for D-06.
  </behavior>
  <action>
    Create `tests/test_rag_grounding.py` with at least two tests and deterministic stubs:
    - `test_historical_answer_uses_retrieved_context`: monkeypatch retriever to return known hit; assert response status is `ready`, message contains `"Историческая сводка"`, and details include non-empty `evidence`.
    - `test_response_contains_traceable_evidence`: assert first evidence item includes `source_id`, `snippet`, `entity_tags`, `rank_score`, and `used_in_answer` is `True`.
    Reuse existing FastAPI test client fixture patterns from `tests/test_api_async_contracts.py`.
  </action>
  <acceptance_criteria>
    - `rg "test_historical_answer_uses_retrieved_context|test_response_contains_traceable_evidence" tests/test_rag_grounding.py` returns both tests.
    - `rg "source_id|snippet|entity_tags|rank_score|used_in_answer" tests/test_rag_grounding.py` returns all traceability fields.
    - `pytest tests/test_rag_grounding.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_grounding.py -q</automated>
  </verify>
  <done>Automated tests prove RAG-01 and RAG-03 via grounded answer generation and traceable evidence linkage.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_rag_grounding.py tests/test_api_async_contracts.py -q` and confirm response details always carry explicit `evidence` array semantics.
</verification>

<success_criteria>
`/next_message` produces evidence-grounded historical responses with explicit traceability, and automated tests validate requirements RAG-01 and RAG-03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-rag-grounding/03-02-SUMMARY.md`
</output>
