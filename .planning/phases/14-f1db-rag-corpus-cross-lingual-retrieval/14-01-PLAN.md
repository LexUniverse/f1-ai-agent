---
phase: 14-f1db-rag-corpus-cross-lingual-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/retrieval/index_builder.py
  - src/retrieval/document_schema.py
  - src/retrieval/retriever.py
  - src/retrieval/alias_resolver.py
  - README.md
  - tests/test_rag_grounding.py
  - tests/test_document_schema.py
autonomous: true
requirements:
  - RAG-08
  - RAG-09
must_haves:
  truths:
    - "CSV_TABLES matches RAG-08 whitelist; noisy large tables excluded."
    - "Race-result chunks carry canonical_entity_id aligned with race:{grand_prix}_gp so Monaco alias filter returns P1 rows."
    - "document_text is English narrative (not raw sorted key-value dump) for indexed tables."
    - "Retriever uses at most two query strings per turn (RU normalized + rule-based EN expansion when year+GP detected)."
    - "Tests prove Monaco 2000 and 2021 champion scenarios surface expected driver ids in top hits (seeded or opt-in full index)."
  artifacts:
    - path: "src/retrieval/document_schema.py"
      provides: "per-table narrative builders; enriched race-result rows"
    - path: "src/retrieval/index_builder.py"
      provides: "race_id lookup from f1db-races; whitelist tables"
    - path: "src/retrieval/retriever.py"
      provides: "multi-query merge + dedupe by source_id"
    - path: "src/retrieval/alias_resolver.py"
      provides: "extra RU aliases / GP coverage as needed for spot checks"
---

<objective>
**Phase 14 (Wave 1):** Перестроить **корпус и индекс** f1db под whitelist (**RAG-08**): осмысленные **EN** чанки, **join** метаданных гонки для **race-results**, исправить **canonical_entity_id** для фильтрации по ГП. Добавить **двухстрочный** retrieval (нормализованный RU + детерминированный EN при наличии года и известного ГП). Закрепить **RAG-09** тестами.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-f1db-rag-corpus-cross-lingual-retrieval/14-CONTEXT.md
@.planning/phases/14-f1db-rag-corpus-cross-lingual-retrieval/14-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whitelist + race lookup in index_builder (RAG-08)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/retrieval/index_builder.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Заменить **`CSV_TABLES`** на явный кортеж из **RAG-08**: `f1db-races.csv`, `f1db-seasons-drivers.csv`, `f1db-drivers.csv`, `f1db-races-race-results.csv`, `f1db-grands-prix.csv`, `f1db-circuits.csv`, `f1db-constructors.csv`, `f1db-seasons.csv`, `f1db-seasons-driver-standings.csv`.
    2. Перед обходом `f1db-races-race-results.csv` прочитать **`f1db-races.csv`** в словарь **`race_id → {grandPrixId, year, officialName, circuitId, …}`** (строковые значения как в CSV).
    3. Перед обходом строк, которым нужны имена ГП/трасс, при необходимости загрузить компактные мапы **`grandPrixId → name/fullName`** из `f1db-grands-prix.csv`, **`circuitId → name`** из `f1db-circuits.csv` (если используются в тексте чанка).
    4. Прокинуть **`race_ctx: dict[str, str] | None`** (или отдельные поля) в сборщик документа для строк **`f1db-races-race-results`**; для остальных таблиц **`None`**.
    5. В **`README.md`** (кратко, 2–4 строки) указать, что список таблиц для индекса задаётся в **`index_builder.py`** и соответствует whitelist v1.5 (детали позже в README_DETAILED).
  </action>
  <acceptance_criteria>
    - `rg "CSV_TABLES" src/retrieval/index_builder.py` показывает все девять имён файлов.
    - `python3 -c "from src.retrieval.index_builder import build_historical_index; print(build_historical_index())"` отрабатывает без исключений при наличии **`f1db-csv/`** (локально у разработчика; в CI не обязателен).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: document_schema — narrative text + canonical for race-results & new tables (RAG-08)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/retrieval/document_schema.py
    - /Users/alexshrestha/Documents/pet/AIAgent/f1db-csv/f1db-races-race-results.csv
    - /Users/alexshrestha/Documents/pet/AIAgent/f1db-csv/f1db-seasons-driver-standings.csv
  </read_first>
  <action>
    1. Расширить публичный контракт: **`build_document_from_row(table_name, row, *, race_context: dict[str, str] | None = None, gp_names: dict[str, str] | None = None, …)`** (минимальный набор правды — только то, что нужно для шаблонов).
    2. Для **`f1db-races-race-results`**: собрать **английский** абзац: официальное имя гонки / ГП, **year**, **round**, финишная позиция, **driverId**, **constructorId**, очки, лучший круг/пол позиции — по мере наличия полей в строке; включить фразы вроде **"Grand Prix"**, **"race winner"** для `positionDisplayOrder==1` или `positionNumber==1`.
    3. Выставить **`canonical_entity_id`**: для строки результата — **`race:{grandPrixId}_gp`** (подчёркивания как в **`alias_resolver`** для `race:monaco_gp`). Если `race_context` отсутствует — не ломать индекс: fallback без canonical или с **`race_event:{raceId}`** (предпочтительно всегда иметь контекст из join).
    4. Добавить в **`metadata`** (строки Chroma): опционально **`year`**, **`grand_prix_id`** (нижний регистр slug как в CSV) для будущих фильтров; типы — строки (Chroma requirement).
    5. Для **`f1db-grands-prix`**, **`f1db-circuits`**, **`f1db-constructors`**, **`f1db-drivers`**, **`f1db-seasons`**, **`f1db-seasons-drivers`**, **`f1db-seasons-driver-standings`**, **`f1db-races`**: заменить сырой дамп на **1–3 предложения** по шаблону таблицы; задать **`canonical_entity_id`** где логично (`gp:{id}`, `circuit:{id}`, `driver:…`, `team:…`, для standings — например **`season:{year}:driver_standings`** или комбинация year + driver для строки).
    6. Удалить или не использовать старый **`_build_document_text`** как единственный путь; сохранить **`source_id`** стабильным (те же правила `_pick_source_id`).
  </action>
  <acceptance_criteria>
    - Новый файл **`tests/test_document_schema.py`**: для синтетической строки race-result с `raceId=653` и `race_context` как у Монако 2000 — в **`document_text`** встречаются подстроки **`david-coulthard`** / **`monaco`** / **`2000`** (или эквивалентные поля из фикстуры); **`canonical_entity_id`** == **`race:monaco_gp`**.
    - Для строки **`f1db-seasons-driver-standings`** с `championshipWon=true`, `year=2021`, `driverId=max-verstappen` — текст содержит **champion** / **2021** / **max-verstappen**.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: retriever — dual query + merge (RAG-08)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/retrieval/retriever.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/retrieval/alias_resolver.py
  </read_first>
  <action>
    1. Добавить **`_retrieval_query_variants(normalized_query: str, canonical_entity_ids: list[str]) -> list[str]`**: всегда **`[normalized_query]`**; если из `normalized_query` извлекается **год `\d{4}`** и среди canonical есть **`race:*_gp`**, добавить **вторую** строку на английском, например **`"{Full GP name or grand prix slug} Grand Prix {year} Formula 1 race result winner"`** — использовать **тот же slug**, что в canonical (например `monaco` → "Monaco Grand Prix {year} …"). Не более **двух** строк.
    2. В **`_query_historical_index`**: вызвать **`collection.query(query_texts=variants, n_results=top_k)`** (если API позволяет) **или** два запроса по одному тексту с **`n_results=top_k`** и объединить: по каждому **`source_id`** оставить **максимальный** `score`, порядок — по убыванию score.
    3. Сохранить **`min_score`** и **`where`** без изменения семантики.
  </action>
  <acceptance_criteria>
    - Unit-тест на **`_retrieval_query_variants`**: для `("гран при монако 2000", ["race:monaco_gp"])` вторая строка содержит **Monaco** и **2000**.
    - `pytest tests/test_rag_grounding.py -q` проходит (при необходимости обновить сиды под новые метаданные).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: alias_resolver — покрытие spot checks (RAG-08)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/retrieval/alias_resolver.py
  </read_first>
  <action>
    1. Добавить RU (и при необходимости латинские) алиасы для частых запросов: **Монако** / **монако гран при** → **`race:monaco_gp`**; формы для **чемпион сезона** / **победитель сезона** не обязаны маппиться в canonical, если год + standings чанк тянется без фильтра — но если фильтр используется, добавить минимальные теги согласованно с метаданными standings.
    2. Не ломать существующие ключи **`_CANONICAL_ALIASES`**.
  </action>
  <acceptance_criteria>
    - Тест: нормализованный запрос содержит алиас Монако → в списке canonical есть **`race:monaco_gp`**.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 5: RAG-09 — интеграционные проверки Monaco 2000 / champion 2021</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_rag_grounding.py
  </read_first>
  <action>
    1. Добавить тест(ы) **`test_retrieval_monaco_2000_winner_in_hits`** и **`test_retrieval_2021_champion_in_hits`**:
       - **Вариант A (предпочтительно для CI):** засеять Chroma **тремя** документами в формате нового билдера (race-result P1 для 653, seasons-driver-standings 2021 champion, опционально race row) и вызвать **`retrieve_historical_context`** с **`min_score=0.0`** или умеренным порогом; assert: в hits есть **`david-coulthard`** / **`max-verstappen`** в snippet или в union топа.
       - **Вариант B (opt-in):** маркер **`@pytest.mark.integration_index`** + полная **`build_historical_index()`** при **`RUN_F1_INDEX_INTEGRATION=1`**.
    2. Задокументировать в **`14-VALIDATION.md`** команды запуска.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_rag_grounding.py tests/test_document_schema.py -q` — зелёный.
  </acceptance_criteria>
</task>

</tasks>
