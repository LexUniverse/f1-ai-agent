---
phase: 14-f1db-rag-corpus-cross-lingual-retrieval
plan: 02
type: execute
wave: 2
depends_on: ["14-01-PLAN.md"]
files_modified:
  - src/retrieval/season_summary_corpus.py
  - src/retrieval/index_builder.py
  - src/retrieval/retriever.py
  - src/retrieval/embeddings.py
  - src/retrieval/query_normalize.py
  - scripts/build_f1_season_summaries.py
  - src/graph/f1_turn_graph.py
  - src/api/chat.py
  - requirements.txt
  - README.md
  - .env.example
  - tests/test_rag_grounding.py
  - tests/test_rag_indexing.py
  - tests/test_query_normalize.py
  - tests/test_api_async_contracts.py
  - tests/test_gigachat_rag.py
  - tests/test_qna_reliability.py
  - tests/conftest.py
removed:
  - src/retrieval/alias_resolver.py
  - tests/test_alias_resolution.py
autonomous: true
requirements:
  - RAG-08
  - RAG-09
must_haves:
  truths:
    - "Корпус — русские сезонные сводки за последние 50 лет: обзор (число пилотов + таблица команда—пилот—место—очки) + отдельный чанк на каждый Гран-при с подиумом и строкой идентификаторов для семантики."
    - "Индексация и запрос к Chroma используют одну модель эмбеддингов: F1_EMBEDDING_MODEL / по умолчанию ai-forever/ru-en-RoSBERTa (sentence-transformers), в т.ч. в pytest."
    - "Алиасы и canonical_entity_id-фильтр в retrieval удалены; запрос в графе — исходный русский текст пользователя."
    - "Источник данных — узкий набор CSV (8 файлов), без построчной индексации всех whitelist-таблиц 14-01."
---

<objective>
**Phase 14 (Wave 2 / replan):** заменить построчный EN-корпус и `alias_resolver` на **компактные русские чанки** по сезонам, **ru-en-RoSBERTa** для индекса и RAG-запроса, полностью **векторный** retrieval без `where` по `canonical_entity_id`.
</objective>

<context>
@.planning/phases/14-f1db-rag-corpus-cross-lingual-retrieval/14-CONTEXT.md (обновлён под wave 2)
@.planning/REQUIREMENTS.md — RAG-08/RAG-09 (интерпретация: качество RU-запросов через модель и текст чанков, не через словарь)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: season_summary_corpus — русские чанки из 8 CSV</name>
  <action>
    Реализовать `build_season_summary_documents(csv_root, years_span=50)`: объединить standings + entrants (команда на пилота), имена из drivers/constructors/grands-prix/countries, гонки и результаты; чанк `season_overview` и чанки `grand_prix_race` с первой строкой «Итог гонки: место 1 …» для попадания победителя в сниппет.
  </action>
  <acceptance_criteria>
    Локально при наличии `f1db-csv/`: число чанков &gt; 0; `pytest tests/test_rag_grounding.py tests/test_query_normalize.py tests/test_rag_indexing.py -q` зелёный.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: embeddings + index_builder + retriever</name>
  <action>
    `get_embedding_function()` в `embeddings.py`; `build_historical_index` только из сводок; `retrieve_historical_context(query, [], …)` — один `query_text`, фильтр только `dataset=f1db`; `min_score` ≈ 0.25.
  </action>
  <acceptance_criteria>
    `python3 scripts/build_f1_season_summaries.py` или `build_historical_index()` без исключений при полном `f1db-csv`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Удалить alias_resolver, API и граф</name>
  <action>
    `query_normalize.normalize_retrieval_query`; `chat.py` и `_node_retrieve` использовать вопрос пользователя для Chroma; удалить `alias_resolver.py` и тесты алиасов; обновить мок-пути в тестах на `normalize_retrieval_query`.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Документация и скрипт CLI</name>
  <action>
    `scripts/build_f1_season_summaries.py`, README, `.env.example` (`F1_EMBEDDING_MODEL`), `sentence-transformers` в requirements.
  </action>
</task>

</tasks>
