---
phase: 04-ru-q-a-answer-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/test_qna_reliability.py
autonomous: true
requirements:
  - QNA-01
  - QNA-02
  - QNA-03
must_haves:
  truths:
    - "Automated tests prove structured details, confidence, and citations on success."
    - "Automated tests prove abstention uses failed status and RETRIEVAL_NO_EVIDENCE with empty evidence."
  artifacts:
    - path: "tests/test_qna_reliability.py"
      provides: "Contract tests for QNA-01..03"
  key_links:
    - from: "tests/test_qna_reliability.py"
      to: "src/api/chat.py"
      via: "TestClient next_message"
      pattern: "next_message"
---

<objective>
Lock Phase 4 behavior with pytest coverage for structured Russian answers, confidence, traceable sources, and insufficient-evidence abstention.

Purpose: Close QNA-01, QNA-02, and QNA-03 with grep- and assert-verifiable API contracts.
Output: `tests/test_qna_reliability.py` integrated with existing fixtures.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/conftest.py
@tests/test_rag_grounding.py
@src/api/chat.py
@.planning/phases/04-ru-q-a-answer-reliability/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QnA reliability contract tests</name>
  <read_first>
    - tests/conftest.py
    - tests/test_rag_grounding.py
    - src/api/chat.py
    - src/models/api_contracts.py
  </read_first>
  <files>tests/test_qna_reliability.py</files>
  <behavior>
    - Reuse the same auth pattern as `test_rag_grounding`: `POST /start_chat` with `{"access_code": "ABC123"}`, then `X-Session-Id` header.
    - Monkeypatch `resolve_entities` like existing grounding tests to decouple from alias CSV.
  </behavior>
  <action>
    Create `tests/test_qna_reliability.py` with:

    1. `test_success_includes_structured_answer_and_confidence(client, monkeypatch)`:
       - Seed session with `session.next_message = "fixture query"`.
       - Monkeypatch `src.api.chat.resolve_entities` to return `("q", ["driver:x"], ["driver:x"])`.
       - Monkeypatch `src.api.chat.retrieve_historical_context` to return a list of one hit dict: `{"source_id": "f1db:test-src", "snippet": "Тестовый отрывок про гонку.", "score": 0.9}` (keys as consumed by `format_evidence`).
       - POST `/next_message`, assert status 200, `payload["status"]=="ready"`.
       - Assert `"structured_answer" in payload["details"]` and `"confidence" in payload["details"]`.
       - Assert `payload["details"]["confidence"]["tier_ru"] == "высокая"` and `payload["details"]["confidence"]["score"] == 0.9`.
       - Assert `payload["details"]["structured_answer"]["sections"]` is a non-empty list and first section `heading == "Сводка"`.
       - Assert `payload["message"]` contains `Историческая сводка:` and `Уверенность:` and the same `tier_ru` substring as in details.
       - Assert `payload["details"]["structured_answer"]["sources_block_ru"]` contains `[1]` and `f1db:test-src`.

    2. `test_no_evidence_remains_failed_retrieval_no_evidence(client, monkeypatch)`:
       - Same setup but monkeypatch `retrieve_historical_context` to return `[]`.
       - Assert `payload["status"]=="failed"`, `payload["details"]["code"]=="RETRIEVAL_NO_EVIDENCE"`, `payload["details"]["evidence"]==[]`.
       - Assert `payload["message"]=="Недостаточно исторических данных в базе f1db."`.
       - Assert `"structured_answer" not in payload["details"]` (or if present as null, prefer absence — implementation should not add structured keys; test must enforce absence).

    3. `test_citation_order_matches_evidence_order(client, monkeypatch)`:
       - Monkeypatch retrieval to return two hits with distinct `source_id` and `score` >= 0.65.
       - Assert `len(payload["details"]["evidence"])==2`.
       - Assert first line after `Источники:` in `sources_block_ru` references `[1]` and matches `evidence[0]["source_id"]` when parsed (substring check: `evidence[0]["source_id"]` appears before `evidence[1]["source_id"]` in `sources_block_ru` in the same order as in `details["evidence"]`).
  </action>
  <acceptance_criteria>
    - `test -f tests/test_qna_reliability.py` exits 0.
    - `rg "test_success_includes_structured_answer|test_no_evidence_remains_failed|test_citation_order" tests/test_qna_reliability.py` returns three matches.
    - `rg "RETRIEVAL_NO_EVIDENCE" tests/test_qna_reliability.py` matches.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_qna_reliability.py -q</automated>
  </verify>
  <done>QNA requirements have dedicated automated coverage.</done>
</task>

</tasks>

<verification_criteria>
- `pytest tests/test_qna_reliability.py -q` exits 0.
- `pytest -q` exits 0 for full regression.
</verification_criteria>
