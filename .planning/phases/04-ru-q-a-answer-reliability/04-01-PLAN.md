---
phase: 04-ru-q-a-answer-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/api_contracts.py
  - src/answer/russian_qna.py
  - src/answer/__init__.py
  - src/api/chat.py
autonomous: true
requirements:
  - QNA-01
  - QNA-02
must_haves:
  truths:
    - "Successful /next_message responses include structured Russian fields in details and explicit Russian confidence."
    - "Citations in the structured payload align by index with details.evidence order."
    - "Insufficient evidence still returns status failed with RETRIEVAL_NO_EVIDENCE unchanged."
  artifacts:
    - path: "src/models/api_contracts.py"
      provides: "Pydantic models for structured QnA sub-shapes used when building details"
    - path: "src/answer/russian_qna.py"
      provides: "Deterministic RU structured answer + confidence from EvidenceItem scores"
    - path: "src/api/chat.py"
      provides: "next_message merges structured QnA into details and RU message line"
  key_links:
    - from: "src/api/chat.py"
      to: "src/answer/russian_qna.py"
      via: "build success-path details"
      pattern: "russian_qna|structured"
---

<objective>
Define typed structured Russian QnA payloads (sections, sources block, confidence) and wire them into `/next_message` success responses while preserving Phase 3 retrieval and Phase 4 abstention semantics.

Purpose: Satisfy QNA-01 and QNA-02 at the API layer with testable, deterministic shaping.
Output: New `src/answer/` module, extended Pydantic models, updated `next_message` success `details` and `message`.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ru-q-a-answer-reliability/04-CONTEXT.md
@.planning/phases/04-ru-q-a-answer-reliability/04-RESEARCH.md
@src/models/api_contracts.py
@src/api/chat.py
@src/retrieval/evidence.py

<interfaces>
`NextMessageResponse.details` remains a `dict`; nested Pydantic models are serialized with `model_dump()` into that dict.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pydantic models and Russian QnA builder</name>
  <read_first>
    - src/models/api_contracts.py
    - src/retrieval/evidence.py
    - .planning/phases/04-ru-q-a-answer-reliability/04-CONTEXT.md
  </read_first>
  <files>src/models/api_contracts.py, src/answer/__init__.py, src/answer/russian_qna.py</files>
  <behavior>
    - D-01/D-02: Structured content in `details`; short RU `message` assembled separately in chat (Task 2) using builder outputs.
    - D-03/D-04: Confidence uses discrete Russian tiers plus numeric score from evidence.
    - D-05/D-06: Numbered sources block uses 1-based indices matching `evidence` list order; `source_id` and snippets stay English/raw in evidence items.
  </behavior>
  <action>
    1. In `src/models/api_contracts.py` add:
       - `QnAConfidence(BaseModel)` with fields `tier_ru: str` and `score: float` (score = max `rank_score` over items, or `0.0` if empty).
       - `AnswerSection(BaseModel)` with `heading: str`, `body: str` (both Russian strings).
       - `StructuredRUAnswer(BaseModel)` with `sections: list[AnswerSection]`, `sources_block_ru: str` (full multiline RU block titled with a line starting exactly `Источники` so clients can grep it), `citation_count: int`.
    2. Create package `src/answer/` with `__init__.py` (may re-export nothing; empty is fine).
    3. Add `src/answer/russian_qna.py` implementing:
       - `CONFIDENCE_HIGH_MIN = 0.65`, `CONFIDENCE_MEDIUM_MIN = 0.40` (float literals exactly as written).
       - `def tier_ru_from_max_score(max_score: float) -> str` returning exactly one of: `"высокая"`, `"средняя"`, `"низкая"` using: `max_score >= 0.65` → высокая; `>= 0.40` → средняя; else низкая.
       - `def build_structured_ru_answer(evidence: list[EvidenceItem]) -> StructuredRUAnswer` where:
         - `sections` has at least one section: `heading="Сводка"`, `body` = first 400 chars of `evidence[0].snippet` + `…` if truncated.
         - `sources_block_ru` lists each item as its own line: `[{n}] {source_id} — {snippet[:80]}` for n from 1..len(evidence), snippets truncated with `…` if longer than 80 chars; first line of `sources_block_ru` must be exactly `Источники:` then newline then lines.
         - `citation_count = len(evidence)`.
    4. Add `def qna_confidence_from_evidence(evidence: list[EvidenceItem]) -> QnAConfidence` returning `tier_ru=tier_ru_from_max_score(max(rank_score))` and `score` = that max (0.0 if no evidence).
  </action>
  <acceptance_criteria>
    - `rg "class QnAConfidence|class AnswerSection|class StructuredRUAnswer" src/models/api_contracts.py` returns all three class definitions.
    - `rg "CONFIDENCE_HIGH_MIN = 0.65" src/answer/russian_qna.py` matches.
    - `rg "def build_structured_ru_answer" src/answer/russian_qna.py` matches.
    - `rg "Источники:" src/answer/russian_qna.py` matches (sources block prefix).
    - `python -c "from src.answer.russian_qna import build_structured_ru_answer, qna_confidence_from_evidence; from src.models.api_contracts import EvidenceItem; e=[EvidenceItem(source_id='f1db:t', snippet='hello world', entity_tags=[], rank_score=0.9)]; a=build_structured_ru_answer(e); c=qna_confidence_from_evidence(e); assert a.citation_count==1 and c.tier_ru=='высокая'"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.answer.russian_qna import build_structured_ru_answer, qna_confidence_from_evidence; from src.models.api_contracts import EvidenceItem; e=[EvidenceItem(source_id='f1db:t', snippet='hello world', entity_tags=[], rank_score=0.9)]; build_structured_ru_answer(e); qna_confidence_from_evidence(e)"</automated>
  </verify>
  <done>Typed models and deterministic RU builder exist and import cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Merge structured QnA into /next_message success path</name>
  <read_first>
    - src/api/chat.py
    - src/answer/russian_qna.py
    - src/models/api_contracts.py
  </read_first>
  <files>src/api/chat.py</files>
  <behavior>
    - D-07/D-08: When `evidence` is empty after formatting, keep `status="failed"`, `details["code"]="RETRIEVAL_NO_EVIDENCE"`, `details["evidence"]=[]`, and user-visible `message` exactly `Недостаточно исторических данных в базе f1db.` — do not add structured QnA keys on that branch.
    - Success branch: `status="ready"`, include existing keys `code` (literal `"OK"`), `normalized_query`, `canonical_entity_ids`, `evidence` as list of dicts from `model_dump()`.
    - Add under the same `details` dict: `structured_answer` = `build_structured_ru_answer(evidence).model_dump()`, `confidence` = `qna_confidence_from_evidence(evidence).model_dump()`.
    - `message` must be a single Russian sentence (or two clauses) that includes the substring `Историческая сводка:` and the substring `Уверенность:` followed by the same `tier_ru` string as in `details["confidence"]["tier_ru"]`. Use the first evidence snippet to build a short summary phrase after `Историческая сводка:` (max 120 chars from snippet, no need for ellipsis logic if shorter).
  </behavior>
  <action>
    In `src/api/chat.py` inside `next_message`, after `format_evidence` and setting `used_in_answer=True`, replace the current success-branch assignment of `message` and `details` with logic that:
    1. Computes `structured = build_structured_ru_answer(evidence)` and `conf = qna_confidence_from_evidence(evidence)`.
    2. Sets `details = {"code": "OK", "normalized_query": normalized_query, "canonical_entity_ids": canonical_entity_ids, "evidence": [item.model_dump() for item in evidence], "structured_answer": structured.model_dump(), "confidence": conf.model_dump()}`.
    3. Sets `message` to: `f"Историческая сводка: {summary}. Уверенность: {conf.tier_ru}."` where `summary` is the first up-to-120 characters of `evidence[0].snippet.strip()`.
    Import builder functions from `src.answer.russian_qna`.
  </action>
  <acceptance_criteria>
    - `rg "build_structured_ru_answer|qna_confidence_from_evidence" src/api/chat.py` matches both symbols.
    - `rg "structured_answer" src/api/chat.py` matches.
    - `rg "RETRIEVAL_NO_EVIDENCE" src/api/chat.py` still matches (abstention path preserved).
    - `rg "Недостаточно исторических данных в базе f1db" src/api/chat.py` matches.
    - `rg "Уверенность:" src/api/chat.py` matches.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_rag_grounding.py::test_next_message_grounds_from_indexed_f1db_context -q</automated>
  </verify>
  <done>Success responses expose structured_answer + confidence; abstention path unchanged.</done>
</task>

</tasks>

<verification_criteria>
- `pytest tests/test_rag_grounding.py -q` passes (regression on inline grounding).
- Manual JSON inspection: `structured_answer.sources_block_ru` starts with `Источники:` and citation lines align with evidence count.
</verification_criteria>
