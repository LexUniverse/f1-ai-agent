---
phase: 06-gigachat-classic-rag
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/answer/gigachat_rag.py
  - src/api/chat.py
  - tests/test_gigachat_rag.py
  - tests/test_qna_reliability.py
  - tests/test_live_enrichment.py
autonomous: true
requirements:
  - GC-02
must_haves:
  truths:
    - "When GigaChat raises or returns unrecoverable errors, /next_message still returns status ready with deterministic template structured_answer and confidence from russian_qna helpers, and details.synthesis.route is exactly template_fallback with optional gigachat_error_class string."
    - "User-visible message on template fallback includes the exact substring GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU from src/answer/gigachat_rag.py (pytest-stable)."
    - "No silent substitution: fallback path never claims LLM synthesis (synthesis.route is never gigachat_rag on fallback)."
    - "Live and historical fallback both set synthesis metadata and disclosure; details.live remains on live fallback when live fetch succeeded."
  artifacts:
    - path: "src/answer/gigachat_rag.py"
      provides: "GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU, GIGACHAT_FALLBACK_ROUTE (optional), append_fallback_disclosure_ru(message: str) -> str"
    - path: "src/api/chat.py"
      provides: "except branches populate synthesis + disclosure for both branches"
    - path: "tests/test_gigachat_rag.py"
      provides: "-k fallback tests"
    - path: "tests/test_live_enrichment.py"
      provides: "monkeypatch gigachat_synthesize_live failure → disclosure + template_fallback"
  key_links:
    - from: "src/api/chat.py"
      to: "src/answer/gigachat_rag.py"
      via: "GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU | append_fallback_disclosure_ru"
      pattern: "GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU|template_fallback"
---

<objective>
Close **GC-02**: on **GigaChat unavailable/errors**, **`/next_message`** uses **deterministic template** synthesis equivalent to pre-Phase-6 helpers, with **machine-readable** **`details["synthesis"]`** (**`route: "template_fallback"`**, **`gigachat_error_class`**) and a **fixed Russian disclosure** in **`message`** (**D-06**). Update integration tests so **live** and **historical** paths assert fallback behavior when the LLM entrypoint is forced to fail.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-gigachat-classic-rag/06-CONTEXT.md
@.planning/phases/06-gigachat-classic-rag/06-RESEARCH.md
@.planning/phases/06-gigachat-classic-rag/06-VALIDATION.md
@src/api/chat.py
@src/answer/gigachat_rag.py
@src/answer/russian_qna.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fallback constants and message disclosure helper</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/06-gigachat-classic-rag/06-CONTEXT.md
  </read_first>
  <action>
    1. In **`src/answer/gigachat_rag.py`** define **`GIGACHAT_FALLBACK_ROUTE = "template_fallback"`** (exact value for **`details["synthesis"]["route"]`** on fallback).
    2. Define **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU`** with **exact literal** (copy verbatim into tests): **`"Ответ подготовлен по шаблону: нейросеть GigaChat сейчас недоступна."`**
    3. Implement **`def append_fallback_disclosure_ru(message: str) -> str`** that returns **`f"{message} — {GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU}"`** when **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU` not in message**, else returns **`message` unchanged**.
  </action>
  <acceptance_criteria>
    - `rg "GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU" src/answer/gigachat_rag.py` exits 0.
    - `rg "GIGACHAT_FALLBACK_ROUTE" src/answer/gigachat_rag.py` exits 0.
    - `rg "def append_fallback_disclosure_ru" src/answer/gigachat_rag.py` exits 0.
    - `python3 -c "from src.answer.gigachat_rag import GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU; assert 'нейросеть' in GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU"` exits 0 from repo root with PYTHONPATH set.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.py fallback synthesis metadata + disclosure</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/russian_qna.py
  </read_first>
  <action>
    1. Import **`GIGACHAT_FALLBACK_ROUTE`**, **`append_fallback_disclosure_ru`** from **`src.answer.gigachat_rag`** into **`src/api/chat.py`**.
    2. **Historical `except` branch** (around **`gigachat_synthesize_historical`**): bind **`exc`**, build **`structured = build_structured_ru_answer(evidence)`**, **`conf = qna_confidence_from_evidence(evidence)`**, **`summary = evidence[0].snippet.strip()[:120]`**, **`base_message = f"Историческая сводка: {summary}. Уверенность: {conf.tier_ru}."`**, set **`message = append_fallback_disclosure_ru(base_message)`**, set **`details["synthesis"] = {"route": GIGACHAT_FALLBACK_ROUTE, "gigachat_error_class": type(exc).__name__}`**.
    3. **Live `except` branch** (around **`gigachat_synthesize_live`**): bind **`exc`**, keep **`summarized`**, **`structured = build_live_structured_ru_answer(summary_ru=summarized)`**, **`conf = live_qna_confidence()`**, **`base_message = live_fresh_user_message_ru(as_of_utc_z=as_of, summary_ru=summarized)`**, **`message = append_fallback_disclosure_ru(base_message)`**, preserve **`details["live"] = LiveDetails(...).model_dump()`** as today, set **`details["synthesis"] = {"route": GIGACHAT_FALLBACK_ROUTE, "gigachat_error_class": type(exc).__name__}`**.
    4. Ensure **success** path still sets **`details["synthesis"]["route"]`** to **`GIGACHAT_SUCCESS_ROUTE`** only (no **`gigachat_error_class`** key on success).
  </action>
  <acceptance_criteria>
    - `rg "GIGACHAT_FALLBACK_ROUTE" src/api/chat.py` exits 0.
    - `rg "append_fallback_disclosure_ru" src/api/chat.py` exits 0.
    - `rg "gigachat_error_class" src/api/chat.py` exits 0.
    - `rg "GIGACHAT_FALLBACK_ROUTE" src/api/chat.py` exits 0 (assign **`route`** from this constant; do not duplicate literal **`"template_fallback"`** in **`chat.py`**).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Automated tests for historical and live fallback</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_live_enrichment.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
  </read_first>
  <action>
    1. In **`tests/test_gigachat_rag.py`** add **`test_historical_fallback_sets_synthesis_route_and_disclosure`**: monkeypatch **`src.api.chat.gigachat_synthesize_historical`** to **`raise RuntimeError("boom")`**; reuse **`/start_chat`** + mocked retrieval from existing patterns; assert **`payload["details"]["synthesis"]["route"] == "template_fallback"`**, **`payload["details"]["synthesis"]["gigachat_error_class"] == "RuntimeError"`**, and **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU in payload["message"]`** (import constant in test file).
    2. In **`tests/test_live_enrichment.py`** extend **`test_live_enriches_when_rag_empty_and_gate_true`**: default path may mock **`gigachat_synthesize_live`** success—add **new test** **`test_live_fallback_on_gigachat_error`**: monkeypatch **`src.api.chat.gigachat_synthesize_live`** to raise **`ValueError("llm")`**; assert **status ready**, **`"live" in details`**, **`details["synthesis"]["route"] == "template_fallback"`**, **`details["synthesis"]["gigachat_error_class"] == "ValueError"`**, and disclosure substring in **`message`**.
    3. Add **`test_success_path_has_no_disclosure_phrase`** (historical): mock **`gigachat_synthesize_historical`** success; assert **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU not in payload["message"]`**.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py::test_historical_fallback_sets_synthesis_route_and_disclosure -x` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py::test_success_path_has_no_disclosure_phrase -x` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_live_enrichment.py::test_live_fallback_on_gigachat_error -x` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -x` exits 0 (full suite gate).
    - `rg "GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU" tests/test_gigachat_rag.py tests/test_live_enrichment.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tighten qna_reliability expectations for synthesis routes</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
  </read_first>
  <action>
    1. Where **`gigachat_synthesize_historical`** is mocked to succeed, assert **`payload["details"]["synthesis"]["route"] == "gigachat_rag"`** (import **`GIGACHAT_SUCCESS_ROUTE`** from **`src.answer.gigachat_rag`** and compare to that symbol, or compare to literal **`"gigachat_rag"`**—pick one style project-wide).
    2. Add one test **`test_historical_template_fallback_includes_disclosure`** mirroring **`test_gigachat_rag`** fallback assertion but kept in **`test_qna_reliability.py`** for Phase 4 regression proximity **or** dedupe by skipping if redundant—**mandatory:** at least **`grep`-verifiable** assertion file **`tests/test_qna_reliability.py`** contains **`template_fallback`** **or** **`GIGACHAT_FALLBACK_ROUTE`** import.
  </action>
  <acceptance_criteria>
    - `rg "template_fallback|GIGACHAT_FALLBACK_ROUTE|gigachat_rag" tests/test_qna_reliability.py` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- Map **`gigachat.exceptions.GigaChatException`** and subclasses explicitly in code comments if narrowing **`except`**—broad **`Exception`** in **`chat.py`** is acceptable if **`gigachat_error_class`** captures the real type for support.
- Phase gate: **`python3 -m pytest`** green before handoff to verification.
