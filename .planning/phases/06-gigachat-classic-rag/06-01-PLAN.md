---
phase: 06-gigachat-classic-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/answer/russian_qna.py
  - src/answer/gigachat_rag.py
  - src/api/chat.py
  - tests/test_gigachat_rag.py
  - tests/test_qna_reliability.py
autonomous: true
requirements:
  - GC-01
  - GC-03
must_haves:
  truths:
    - "src/answer/gigachat_rag.py owns sync GigaChat completion, prompt assembly with enumerated chunks [1]..[n] matching details.evidence order, and hybrid mapping to StructuredRUAnswer + QnAConfidence (confidence from qna_confidence_from_evidence or live_qna_confidence, not raw LLM scores)."
    - "Historical and live success branches in src/api/chat.py call gigachat_rag entrypoints before assembling NextMessageResponse; details retain evidence and, on live path, details.live unchanged from Phase 5."
    - "src/answer/russian_qna.py remains the home of deterministic builders and summarization helpers; chat.py does not call build_structured_ru_answer as the primary success path (GC-03)."
    - "pytest exercises the GigaChat path via monkeypatch only (no real network); dependency gigachat==0.2.0 is pinned in requirements.txt."
  artifacts:
    - path: "requirements.txt"
      provides: "gigachat==0.2.0 pin"
    - path: "src/answer/gigachat_rag.py"
      provides: "gigachat_synthesize_historical, gigachat_synthesize_live, GIGACHAT_SUCCESS_ROUTE"
    - path: "src/api/chat.py"
      provides: "orchestration try/except shell; success details synthesis.route=gigachat_rag"
    - path: "tests/test_gigachat_rag.py"
      provides: "mocked SDK / entrypoint coverage for GC-01 happy path"
  key_links:
    - from: "src/api/chat.py"
      to: "src/answer/gigachat_rag.py"
      via: "gigachat_synthesize_historical | gigachat_synthesize_live"
      pattern: "gigachat_synthesize_"
    - from: "src/answer/gigachat_rag.py"
      to: "src/answer/russian_qna.py"
      via: "qna_confidence_from_evidence, live_qna_confidence, deterministic sources_block"
      pattern: "qna_confidence_from_evidence|live_qna_confidence"
---

<objective>
Establish **`src/answer/gigachat_rag.py`** as the primary RAG synthesis module (GC-03), wire **`/next_message`** so that with evidence or live success the Russian payload is produced via **GigaChat** prompts that include **retrieved chunks** or **live summary text** (GC-01). Add a **root `requirements.txt`** pin for **`gigachat==0.2.0`** and **pytest** that mocks the LLM (no credentials). Template fallback **signaling** (GC-02) is **deferred** to `06-02-PLAN.md`; this wave may use a **silent** template fallback on failure only to avoid crashing production until Wave 2 adds `route` + disclosure, or **re-raise** after logging—**mandatory:** implement **`try` GigaChat / `except Exception` → deterministic `build_structured_ru_answer` / `build_live_structured_ru_answer` + legacy `message` shape** so behavior stays safe, without yet adding **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU`** or **`template_fallback`** route (those are Wave 2 only).
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-gigachat-classic-rag/06-CONTEXT.md
@.planning/phases/06-gigachat-classic-rag/06-RESEARCH.md
@src/models/api_contracts.py
@src/api/chat.py
@src/answer/russian_qna.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependency pin and shared deterministic sources block</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/russian_qna.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/06-gigachat-classic-rag/06-RESEARCH.md
  </read_first>
  <action>
    1. Create `/Users/alexshrestha/Documents/pet/AIAgent/requirements.txt` with line **`gigachat==0.2.0`** (add other runtime deps only if already implied by imports project-wide—minimal: **only gigachat pin** unless repo already lists httpx/fastapi elsewhere; if an existing manifest is discovered, merge pin there instead and omit duplicate requirements.txt—**if no manifest exists, this file is only `gigachat==0.2.0`**).
    2. In `src/answer/russian_qna.py` add **`def build_sources_block_ru_from_evidence(evidence: list[EvidenceItem]) -> tuple[str, int]`** that returns **`(sources_block_ru, citation_count)`** using the **same line format** as `build_structured_ru_answer` (lines starting with **`Источники:`**, entries **`[{n}] {source_id} — {truncated_snippet}`** with **`_truncate(..., 80)`**).
    3. Refactor **`build_structured_ru_answer`** to call **`build_sources_block_ru_from_evidence`** for `sources_block_ru` and `citation_count` to avoid duplication.
  </action>
  <acceptance_criteria>
    - `rg "^gigachat==0\.2\.0$" /Users/alexshrestha/Documents/pet/AIAgent/requirements.txt` exits 0 (or document in task done that manifest X contains the same pin).
    - `rg "def build_sources_block_ru_from_evidence" src/answer/russian_qna.py` exits 0.
    - `python3 -c "from src.answer.russian_qna import build_sources_block_ru_from_evidence; print('ok')"` runs from repo root with `PYTHONPATH=.` or project venv equivalent exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gigachat_rag module — client, prompts, hybrid assembly</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/russian_qna.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/06-gigachat-classic-rag/06-CONTEXT.md
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/06-gigachat-classic-rag/06-RESEARCH.md
  </read_first>
  <action>
    1. Create **`src/answer/gigachat_rag.py`** with module constant **`GIGACHAT_SUCCESS_ROUTE = "gigachat_rag"`** (string value **exactly** for `details["synthesis"]["route"]` on LLM success).
    2. Implement **`@dataclass class GigachatRUSynthesisResult:`** with fields **`message: str`**, **`structured_answer: StructuredRUAnswer`**, **`confidence: QnAConfidence`** (import models from `src.models.api_contracts`).
    3. Implement **`gigachat_synthesize_historical(*, evidence: list[EvidenceItem], user_question: str) -> GigachatRUSynthesisResult`**:
       - Build user prompt text listing each evidence item as **`[i] source_id: snippet`** for **i 1..len(evidence)** matching **`EvidenceItem` order**.
       - Use **`gigachat.GigaChat`** context manager with env-driven auth per README: rely on **`GIGACHAT_CREDENTIALS`**; set constructor **`timeout`** from **`float(os.environ.get("GIGACHAT_TIMEOUT", "30"))`**, **`max_retries`** from **`int(os.environ.get("GIGACHAT_MAX_RETRIES", "2"))`**, **`verify_ssl_certs`** from **`os.environ.get("GIGACHAT_VERIFY_SSL_CERTS", "true").lower() == "true"`**.
       - Request model **`os.environ.get("GIGACHAT_MODEL", "GigaChat")`** via `Chat`/`Messages` API per SDK README.
       - Instruct model (system + user) to return **JSON only** with keys **`message`** (short RU string), **`sections`** (list of **`{"heading": str, "body": str}`**). Parse with **`json.loads`**; validate section bodies with **`AnswerSection.model_validate`**; on **`json.JSONDecodeError`** or **`ValidationError`**, perform **one** repair attempt: second message with validation error text; if still invalid, **re-raise** the last exception (chat layer falls back in Task 3).
       - Set **`sources_block_ru, citation_count = build_sources_block_ru_from_evidence(evidence)`** and **`confidence = qna_confidence_from_evidence(evidence)`** regardless of model output; build **`StructuredRUAnswer(sections=..., sources_block_ru=sources_block_ru, citation_count=citation_count)`**.
    4. Implement **`gigachat_synthesize_live(*, summary_ru: str, user_question: str, as_of_utc_z: str) -> GigachatRUSynthesisResult`** similarly: user prompt must include label **`Актуальные данные (не из исторической базы):`** plus **`summary_ru`** and **`as_of_utc_z`**; **no** fabricated historical chunks; after parse, set **`confidence = live_qna_confidence()`** and **`structured_answer`** using **`build_live_structured_ru_answer(summary_ru=summary_ru)`** for **`sources_block_ru`/`citation_count`** (sections from model JSON only).
  </action>
  <acceptance_criteria>
    - `rg "GIGACHAT_SUCCESS_ROUTE" src/answer/gigachat_rag.py` exits 0.
    - `rg "def gigachat_synthesize_historical" src/answer/gigachat_rag.py` exits 0.
    - `rg "def gigachat_synthesize_live" src/answer/gigachat_rag.py` exits 0.
    - `rg "build_sources_block_ru_from_evidence" src/answer/gigachat_rag.py` exits 0.
    - `python3 -c "from src.answer.gigachat_rag import GIGACHAT_SUCCESS_ROUTE"` exits 0 from repo root with correct PYTHONPATH.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat.py orchestration and success synthesis metadata</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/russian_qna.py
  </read_first>
  <action>
    1. In **`src/api/chat.py`**, **`from src.answer.gigachat_rag import GIGACHAT_SUCCESS_ROUTE, gigachat_synthesize_historical, gigachat_synthesize_live`** (adjust imports if names differ—keep **public names** **`gigachat_synthesize_historical`** and **`gigachat_synthesize_live`** for test monkeypatching).
    2. **Historical branch** (`if evidence:`): replace direct **`build_structured_ru_answer` / `qna_confidence_from_evidence` / f-string Историческая сводка** success path with:
       - **`try:`** `result = gigachat_synthesize_historical(evidence=evidence, user_question=_session.next_message)` **except Exception:** call **`build_structured_ru_answer(evidence)`**, **`qna_confidence_from_evidence(evidence)`**, and build **`message`** as today’s template **`f"Историческая сводка: {summary}. Уверенность: {conf.tier_ru}."`** (first snippet truncate 120)—**no** `synthesis` sub-dict yet on fallback (Wave 2).
       - On success: **`message = result.message`**, **`structured = result.structured_answer`**, **`conf = result.confidence`**; set **`details["synthesis"] = {"route": GIGACHAT_SUCCESS_ROUTE}`** (nested dict only on GigaChat success).
    3. **Live success branch**: after **`summarized = summarize_live_next_payload_ru(live_payload)`**, **`try:`** `result = gigachat_synthesize_live(summary_ru=summarized, user_question=_session.next_message, as_of_utc_z=as_of)` **except Exception:** use existing **`live_fresh_user_message_ru`**, **`build_live_structured_ru_answer`**, **`live_qna_confidence()`** as today.
       - On success: **`message`** should remain a **short** RU line; use **`result.message`** if non-empty else **`live_fresh_user_message_ru(as_of_utc_z=as_of, summary_ru=summarized)`**; include **`details["live"]`** unchanged; add **`details["synthesis"] = {"route": GIGACHAT_SUCCESS_ROUTE}`** on LLM success only.
    4. Preserve **Phase 4/5** failure codes and **`RETRIEVAL_NO_EVIDENCE` / `LIVE_UNAVAILABLE`** branches **unchanged**.
  </action>
  <acceptance_criteria>
    - `rg "gigachat_synthesize_historical" src/api/chat.py` exits 0.
    - `rg "gigachat_synthesize_live" src/api/chat.py` exits 0.
    - `rg "synthesis" src/api/chat.py` exits 0.
    - `rg "GIGACHAT_SUCCESS_ROUTE" src/api/chat.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tests — gigachat_rag unit + qna_reliability monkeypatch</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/conftest.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
  </read_first>
  <action>
    1. Add **`tests/test_gigachat_rag.py`** with at least:
       - **`test_gigachat_synthesize_historical_returns_hybrid_structured_answer`**: monkeypatch **`src.answer.gigachat_rag.GigaChat`** (or **`gigachat_synthesize_historical`’s** inner client call site) to return fixed JSON **message** + **sections**; pass one **`EvidenceItem`**; assert **`result.confidence`** equals **`qna_confidence_from_evidence(evidence)`** and **`sources_block_ru`** contains **`[1]`** and **source_id**.
       - **`test_chat_historical_next_message_uses_synthesis_route_when_mocked`**: **`monkeypatch.setattr("src.api.chat.gigachat_synthesize_historical", fake)`** returning a **`GigachatRUSynthesisResult`**; POST **`/next_message`**; assert **`payload["details"]["synthesis"]["route"] == "gigachat_rag"`** and mocked **message** in **`payload["message"]`**.
    2. Update **`tests/test_qna_reliability.py`** **`test_success_includes_structured_answer_and_confidence`** (and **`test_citation_order_matches_evidence_order`**): monkeypatch **`src.api.chat.gigachat_synthesize_historical`** to return deterministic **`GigachatRUSynthesisResult`** so assertions no longer depend on template strings **`Историческая сводка:`** unless testing fallback; keep assertions on **`tier_ru`**, **`sources_block`**, **`[n]`** order, and **`synthesis.route` == `gigachat_rag`** when mock used.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py -x` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py -x` exits 0.
    - `rg "gigachat_synthesize_historical" tests/test_qna_reliability.py` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- **Do not** add **`GIGACHAT_TEMPLATE_FALLBACK_DISCLOSURE_RU`** or **`route: template_fallback`** in this plan (Wave 2).
- After merge, run **`python3 -m pytest`** from repo root.
