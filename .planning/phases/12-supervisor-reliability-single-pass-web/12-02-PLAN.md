---
phase: 12-supervisor-reliability-single-pass-web
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01-PLAN.md"
files_modified:
  - src/models/api_contracts.py
  - src/api/chat.py
  - tests/test_next_message_contract_phase9.py
  - tests/
autonomous: true
requirements:
  - WEB-02
must_haves:
  truths:
    - "`out_details` from finalize includes `provenance` dict with rag summary, web (queries, results), optional fetch `{url, ok, excerpt_preview?}`, and synthesis echo (WEB-02)."
    - "`assemble_next_message_details` copies `provenance` into API `details` when present; keeps existing `evidence`, `structured_answer`, `web` for backward compatibility."
    - "No duplicate URL lists inside `provenance.web` vs legacy `details.web` beyond documented alias rule (same list reference or explicit note in code comment)."
  artifacts:
    - path: "src/api/chat.py"
      provides: "details.provenance for Phase 13 Streamlit"
    - path: "src/models/api_contracts.py"
      provides: "optional Pydantic models for provenance documentation"
---

<objective>
**Phase 12 Wave 2:** **WEB-02** — add **`provenance`** to graph output and **`/next_message` `details`**, typed in Pydantic where helpful, **without breaking** existing Phase 9 tests that assert `evidence`, `structured_answer`, **`web`**.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-supervisor-reliability-single-pass-web/12-CONTEXT.md
@src/api/chat.py
@src/graph/f1_turn_graph.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic — ProvenanceSnapshot (optional but recommended)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
  </read_first>
  <action>
    1. Add models e.g. **`ProvenanceRag`**, **`ProvenanceWebFetch`**, **`ProvenanceWeb`**, **`ProvenanceSnapshot`** (nested: `rag`, `web`, `synthesis`) matching fields **`finalize_accept`** / graph can populate. Keep fields **JSON-serializable** and stable for Streamlit Phase 13.
    2. Document in docstrings that **`details["provenance"]`** follows this shape.
  </action>
  <acceptance_criteria>
    - `rg "class ProvenanceSnapshot" src/models/api_contracts.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Graph finalize — build provenance dict</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
  </read_first>
  <action>
    1. In **`_node_finalize_accept`** and **`_node_finalize_fail`**, set **`details["provenance"]`** as a dict:
       - **`rag`**: `normalized_query` from state, compact **evidence** list (source_id, snippet truncated, entity_tags) — avoid huge payloads.
       - **`web`**: `queries`, `results` (same structure as today’s `web_results` rows), optional **`fetch`**: `{url, ok, error?, excerpt_preview?}` from state if fetch ran.
       - **`synthesis`**: same as nested **`synthesis`** already in details.
    2. Ensure **`provenance`** is omitted or minimal when **no web** was used (RAG-only success): still include **`rag`** + **`synthesis`** for Phase 13 unified expander.
  </action>
  <acceptance_criteria>
    - `rg '"provenance"' src/graph/f1_turn_graph.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat.py — pass provenance through</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
  </read_first>
  <action>
    1. In **`assemble_next_message_details`**, if **`out_details.get("provenance")`** is a **`dict`**, set **`details["provenance"] = ...`** (validate with **`ProvenanceSnapshot.model_validate`** then **`.model_dump()`** if models exist, else pass through dict).
    2. Preserve existing **`web`**, **`evidence`**, **`structured_answer`** behavior from Phase 9.
  </action>
  <acceptance_criteria>
    - `rg "provenance" /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py` exits 0.
    - `rg -i "confidence" /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py` exits 1.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests — provenance in details JSON</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_next_message_contract_phase9.py
  </read_first>
  <action>
    1. Extend or add **`tests/test_phase12_provenance.py`**: build **`details`** via **`assemble_next_message_details`** with mocked **`out_details`** containing **`provenance`** with **`rag`** and **`web`**; **`json.dumps`** must contain **`provenance`** and no **`confidence`**.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -q --ignore-glob='*integration*'` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- Phase **13** Streamlit will **read `details["provenance"]`** first; keep legacy fields until UI migration is complete.
