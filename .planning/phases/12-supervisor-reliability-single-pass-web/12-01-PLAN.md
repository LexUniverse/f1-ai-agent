---
phase: 12-supervisor-reliability-single-pass-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/answer/gigachat_rag.py
  - src/graph/f1_turn_graph.py
  - src/graph/page_fetch.py
  - tests/test_f1_supervisor_graph.py
  - tests/test_f1_assistant_graph.py
autonomous: true
requirements:
  - AGT-06
  - AGT-07
  - SRCH-04
must_haves:
  truths:
    - "Supervisor accept path has no numeric threshold; audit documented in code comment or AGT-06 note in gigachat_rag."
    - "gigachat_supervisor_accept_answer performs one JSON repair round on parse/validation failure before returning False (AGT-06)."
    - "After RAG rejection, at most one run_tavily_search_once per turn; _route_after_supervisor uses web_search_rounds >= 1 (not 2) for finalize_fail (AGT-07)."
    - "Between Tavily and final web synthesis: GigaChat chooses best URL and whether title/snippet fields suffice; if not, one bounded httpx GET extracts text for synthesis (SRCH-04)."
    - "If Tavily errors, existing WEB_SEARCH_UNAVAILABLE path unchanged (no fetch)."
  artifacts:
    - path: "src/graph/page_fetch.py"
      provides: "fetch_url_text_plain with timeout and size cap"
    - path: "src/graph/f1_turn_graph.py"
      provides: "single Tavily cap, web_plan/fetch nodes, AGT-05 after one web-backed reject"
    - path: "src/answer/gigachat_rag.py"
      provides: "supervisor repair; web planning JSON; synthesis with optional fetched excerpt"
---

<objective>
**Phase 12 Wave 1:** **AGT-06** (supervisor repair + audit note + optional `F1_LOG_SUPERVISOR_DECISIONS` logging), **AGT-07** (one Tavily per turn; **finalize_fail** when `web_search_rounds >= 1` and supervisor still rejects after web-backed candidate), **SRCH-04** (LLM plans URL + title-sufficiency; optional **single-page fetch** via new `page_fetch` module before `agent1_web`).
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-supervisor-reliability-single-pass-web/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Supervisor — one repair round + optional log (AGT-06)</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
  </read_first>
  <action>
    1. Refactor **`gigachat_supervisor_accept_answer`** so the first JSON parse/validation failure triggers **one** follow-up GigaChat user message (same pattern as `_chat_completion_json` repair) demanding **only** `{"accept": true}` or `{"accept": false}`.
    2. If the second attempt still fails → return **`False`**.
    3. If env **`F1_LOG_SUPERVISOR_DECISIONS=1`** (truthy string): log **`INFO`** with **`accept`** boolean and **truncated** `candidate_answer` (e.g. first 120 chars) — not full user question in log unless already acceptable for your privacy model; prefer omitting `user_question` from logs.
    4. Add short **module comment** near supervisor: numeric **retrieval scores** and **`tier_ru_from_max_score`** are **not** used for supervisor accept (AGT-06 audit pointer).
  </action>
  <acceptance_criteria>
    - `rg "def gigachat_supervisor_accept_answer" -A35 src/answer/gigachat_rag.py` shows a second `client.chat` or messages append for repair path.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py -q` passes (add unit test with monkeypatch returning bad JSON once then good, if no existing coverage).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GigaChat — web plan JSON + synthesis with optional excerpt</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
  </read_first>
  <action>
    1. Add Pydantic model **`_WebPlanPayload`**: **`best_url: str`**, **`titles_sufficient: bool`** (optional: **`reason: str`** for logging only).
    2. Add **`gigachat_plan_web_use(*, user_question: str, web_results: list[dict]) -> _WebPlanPayload`** using GigaChat JSON: system prompt instructs pick **one** best URL from the list for the question and whether **title+snippet (Tavily content)** is enough to answer **without** fetching the page.
    3. Extend **`gigachat_synthesize_from_web_results`** with optional **`fetched_page_excerpt: str | None = None`**: when non-empty, append a labeled block to the user prompt so the model can use **page text** plus prior snippets.
  </action>
  <acceptance_criteria>
    - `rg "def gigachat_plan_web_use" src/answer/gigachat_rag.py` exits 0.
    - `rg "fetched_page_excerpt" src/answer/gigachat_rag.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: page_fetch — httpx GET + HTML → plain text</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/pyproject.toml
    - /Users/alexshrestha/Documents/pet/AIAgent/requirements.txt
  </read_first>
  <action>
    1. Create **`src/graph/page_fetch.py`** with **`fetch_url_text_plain(url: str, *, timeout_s: float = 15.0, max_bytes: int = 500_000) -> str`** using **httpx** (project already uses httpx in Streamlit/API paths).
    2. Use **`html.parser`** (`html.parser.HTMLParser` subclass or walk with **BeautifulSoup** only if already a dependency — **prefer stdlib** `html.parser` to strip tags into rough plain text).
    3. On non-200, timeout, or oversize: return **empty string** (caller treats as failed fetch); do not raise to graph unless you document it.
    4. Set a clear **User-Agent** string (e.g. `F1Assistant/1.4 (+local)`).
  </action>
  <acceptance_criteria>
    - `test -f /Users/alexshrestha/Documents/pet/AIAgent/src/graph/page_fetch.py` exits 0.
    - Add **`tests/test_page_fetch.py`** with **httpx.MockTransport** returning small HTML → nonempty stripped text; `python3 -m pytest tests/test_page_fetch.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: f1_turn_graph — single Tavily, plan → fetch? → synthesize → supervisor</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/12-supervisor-reliability-single-pass-web/12-CONTEXT.md
  </read_first>
  <action>
    1. Change **`_route_after_supervisor`**: **`finalize_fail`** when **`int(state.get("web_search_rounds", 0)) >= 1`** and not accept (replace **`>= 2`**).
    2. Insert nodes after **`tavily_search`** success path: **`web_plan`** → conditional **`fetch_page`** (only if `titles_sufficient` is False and `best_url` non-empty) → **`agent1_web`**.
    3. **`web_plan`**: call **`gigachat_plan_web_use`**; store **`web_plan_best_url`**, **`web_titles_sufficient`** on state (`TypedDict` fields).
    4. **`fetch_page`**: if needed, set **`fetched_page_excerpt`** (truncated string, e.g. 8–12k chars) on state; on failure leave excerpt empty.
    5. **`agent1_web`**: call **`gigachat_synthesize_from_web_results`** with **`fetched_page_excerpt`** from state when present.
    6. Extend **`F1TurnState`** with new keys used above; keep **`web_search_rounds`** increment **only in `tavily_search`** (max **1**).
    7. Update **`tests/test_f1_supervisor_graph.py`**: **`test_two_tavily_then_agt05`** → rename/replace to assert **one** Tavily max and AGT-05 after supervisor rejects **web-backed** candidate once; adjust mocks for new nodes.
    8. Update **`tests/test_f1_assistant_graph.py`** for graph shape if needed.
  </action>
  <acceptance_criteria>
    - `rg "web_search_rounds" src/graph/f1_turn_graph.py` exits 0.
    - `rg "web_search_rounds.*>= 1" src/graph/f1_turn_graph.py` exits 0 (AGT-05 after one web pass).
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_f1_supervisor_graph.py /Users/alexshrestha/Documents/pet/AIAgent/tests/test_f1_assistant_graph.py -q` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/ -q --ignore-glob='*integration*'` exits 0 (fix any broken mocks).
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- **LangGraph wiring:** Prefer **explicit nodes** (`web_plan`, `fetch_page`) over a monolith so tests can monkeypatch per node.
- **Offline tests:** Monkeypatch **`gigachat_plan_web_use`**, **`fetch_url_text_plain`**, **`gigachat_synthesize_from_web_results`** as needed.
