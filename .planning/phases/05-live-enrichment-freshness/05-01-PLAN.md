---
phase: 05-live-enrichment-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/api_contracts.py
  - src/integrations/__init__.py
  - src/integrations/f1api_client.py
  - src/live/__init__.py
  - src/live/gate.py
  - tests/test_live_gate.py
  - tests/test_f1api_client.py
autonomous: true
requirements:
  - LIVE-02
  - LIVE-03
must_haves:
  truths:
    - "Pydantic LiveDetails carries UTC Z as_of and provider literal f1api.dev for JSON details.live."
    - "F1ApiClient reads F1API_BASE_URL (default https://f1api.dev) and F1API_TIMEOUT_SECONDS (default 10) and maps timeouts, transport errors, 5xx, and open breaker to LiveUpstreamError."
    - "requires_live_data gate is deterministic from RU/EN allowlists with pytest-locked positives and negatives."
  artifacts:
    - path: "src/models/api_contracts.py"
      provides: "LiveDetails (and LiveUpstreamError-free shapes only here)"
    - path: "src/integrations/f1api_client.py"
      provides: "httpx sync client, bounded GET retries, circuit breaker, fetch_current_next"
    - path: "src/live/gate.py"
      provides: "requires_live_data(normalized_query, raw_user_text)"
    - path: "tests/test_live_gate.py"
      provides: "Parametrized gate coverage"
    - path: "tests/test_f1api_client.py"
      provides: "MockTransport coverage for success, 503 retry, breaker open"
  key_links:
    - from: "src/integrations/f1api_client.py"
      to: "https://f1api.dev"
      via: "httpx.Client base_url"
      pattern: "F1API_BASE_URL|f1api\\.dev"
    - from: "src/live/gate.py"
      to: "src/api/chat.py"
      via: "import requires_live_data (wired in 05-02)"
      pattern: "requires_live_data"
---

<objective>
Add typed `LiveDetails`, a resilient sync `F1ApiClient` for f1api.dev (httpx, ≤10s default timeout, bounded retries, circuit breaker), and a pure deterministic live-intent gate. No `/next_message` wiring in this plan (05-02).

Purpose: Satisfy LIVE-02 and LIVE-03 foundations per D-05, D-12, D-13; honor D-03 (allowlist, no LLM routing).
Output: New integration + live packages, extended contracts, unit tests with mocked HTTP.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-enrichment-freshness/05-CONTEXT.md
@.planning/phases/05-live-enrichment-freshness/05-RESEARCH.md
@src/models/api_contracts.py

<interfaces>
From `src/models/api_contracts.py` (existing): `BaseModel`, `Field`, `Literal` patterns for nested `details` dict serialization via `model_dump()` in routes.

New exports (this plan): `LiveDetails` with `as_of: str`, `provider: Literal["f1api.dev"]`, optional `endpoint_key: str | None = None` (Claude’s discretion per D-05 — default `None`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LiveDetails model and deterministic live gate</name>
  <read_first>
    - src/models/api_contracts.py
    - .planning/phases/05-live-enrichment-freshness/05-CONTEXT.md
    - .planning/phases/05-live-enrichment-freshness/05-RESEARCH.md
  </read_first>
  <files>src/models/api_contracts.py, src/live/__init__.py, src/live/gate.py, tests/test_live_gate.py</files>
  <behavior>
    - D-03: `requires_live_data(*, normalized_query: str, raw_user_text: str) -> bool` returns True iff either string (lowercased for matching) hits the allowlist below; no ML/LLM.
    - D-05/D-13: `LiveDetails` requires `as_of` matching `^\\d{4}-\\d{2}-\\d{2}T` and ending with literal `Z` at validation time **or** document optional relaxed validation — **mandatory for this task:** validate with Pydantic `Field(pattern=r'^\\d{4}-\\d{2}-\\d{2}T.*Z$')` on `as_of`.
    - Gate false for purely historical phrasing without live cues (e.g. “who won monaco 1996”) in tests.
  </behavior>
  <action>
    1. In `src/models/api_contracts.py` add `LiveDetails(BaseModel)` with:
       - `as_of: str = Field(..., pattern=r'^\\d{4}-\\d{2}-\\d{2}T.*Z$')`
       - `provider: Literal["f1api.dev"] = "f1api.dev"`
       - `endpoint_key: str | None = None`
    2. Create `src/live/__init__.py` (empty or re-export `requires_live_data` only).
    3. Create `src/live/gate.py` implementing `requires_live_data(*, normalized_query: str, raw_user_text: str) -> bool`:
       - Define module-level tuples (exact spellings for grep/tests):
         - `RU_LIVE_SUBSTRINGS = ("сейчас", "актуальн", "текущ", "последн", "календар", "следующ", "график", "на данный момент")`
         - `EN_LIVE_PHRASES = ("next race", "this season", "current season")`
       - For English single-token cues use **word-boundary** regex on the concatenation `haystack = f"{normalized_query}\\n{raw_user_text}"` lowered: `re.findall` or `re.search` with pattern `\\b(current|now|latest|standings|schedule|calendar)\\b` (case-insensitive).
       - Return True if any `s in haystack_lower` for RU substrings OR any EN phrase substring in haystack_lower OR regex match.
    4. Add `tests/test_live_gate.py` with `@pytest.mark.parametrize` covering at least:
       - True: `("кто лидирует сейчас", "кто лидирует сейчас")`
       - True: `("current f1 standings", "current f1 standings")`
       - True: `("calendar next race", "calendar next race")` (phrase + keyword overlap ok)
       - False: `("who won monaco 1996", "who won monaco 1996")`
       - False: `("ferrari history", "ferrari history")`
  </action>
  <acceptance_criteria>
    - `rg "class LiveDetails" src/models/api_contracts.py` matches.
    - `rg "def requires_live_data" src/live/gate.py` matches.
    - `rg "RU_LIVE_SUBSTRINGS" src/live/gate.py` matches.
    - `rg "EN_LIVE_PHRASES" src/live/gate.py` matches.
    - `python3 -m pytest tests/test_live_gate.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -m pytest tests/test_live_gate.py -x</automated>
  </verify>
  <done>LiveDetails validates UTC-Z-shaped as_of; gate allowlist is code-defined and fully covered by parametrized tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F1ApiClient with httpx, retries, breaker, and MockTransport tests</name>
  <read_first>
    - .planning/phases/05-live-enrichment-freshness/05-RESEARCH.md
    - src/models/api_contracts.py
  </read_first>
  <files>src/integrations/__init__.py, src/integrations/f1api_client.py, tests/test_f1api_client.py</files>
  <action>
    - D-12: Default timeout **10.0** seconds total (use `httpx.Timeout(10.0)` or connect/read tuple that keeps max ≤10.0); override via `F1API_TIMEOUT_SECONDS` float parsed from env.
    - Env vars: `F1API_BASE_URL` default `"https://f1api.dev"`, `F1API_TIMEOUT_SECONDS` default `"10"`, optional `F1API_BREAKER_FAIL_MAX` default `5`, `F1API_BREAKER_COOL_SECONDS` default `30`, `F1API_HTTP_RETRY_MAX` default `2` (application retries for **GET** on `httpx.ReadTimeout` and HTTP **5xx** after `raise_for_status`, not for 4xx except optionally — **mandatory:** retry only when `status_code >= 500` or timeout; **do not** retry on 404).
    - Use `httpx.HTTPTransport(retries=1)` for connection-level retries (ConnectError/ConnectTimeout) in addition to application retries.
    - Circuit breaker: **manual** implementation in-module (no new PyPI deps). After `breaker_fail_max` **consecutive** failures that would map to `LiveUpstreamError`, open circuit for `breaker_cool_seconds` and raise `LiveUpstreamError("circuit_open")` without calling httpx. Successful GET resets consecutive count to 0.
    - D-13: `as_of_utc_z()` helper function in module: `def as_of_utc_z_from_completion(when: datetime | None = None) -> str` returning `datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")` when `when` is None, else normalize `when` to UTC and ensure output ends with `Z`.
    - Public exception: `class LiveUpstreamError(Exception): ...` in `f1api_client.py` (not in api_contracts).
    - Public class `F1ApiClient` with `@classmethod def from_env(cls) -> F1ApiClient` reading env vars.
    - Method `def fetch_current_next(self) -> tuple[dict, str]` performs `GET /api/current/next` relative to base URL, parses JSON, returns `(data, as_of)` where `as_of` is `as_of_utc_z_from_completion()` at **successful response completion** (no fabricated API timestamp).
    - Non-2xx after retries: raise `LiveUpstreamError` with `status_code` attribute set when HTTP error.
  </action>
  <acceptance_criteria>
    - `rg "class F1ApiClient" src/integrations/f1api_client.py` matches.
    - `rg "class LiveUpstreamError" src/integrations/f1api_client.py` matches.
    - `rg "F1API_BASE_URL" src/integrations/f1api_client.py` matches.
    - `rg "F1API_TIMEOUT_SECONDS" src/integrations/f1api_client.py` matches.
    - `rg "HTTPTransport" src/integrations/f1api_client.py` matches.
    - `python3 -m pytest tests/test_f1api_client.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -m pytest tests/test_f1api_client.py -x</automated>
  </verify>
  <done>Client is mock-tested for 200 JSON + as_of ending in Z, 503 then success retry path, and breaker open short-circuit.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_live_gate.py tests/test_f1api_client.py -x`. No real network calls in pytest (MockTransport only).
</verification>

<success_criteria>
Live contracts and integration client exist with env-driven configuration, deterministic gate, and automated tests proving retry/breaker behavior under mocks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-enrichment-freshness/05-01-SUMMARY.md`
</output>
