---
phase: 05-live-enrichment-freshness
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/main.py
  - src/api/chat.py
  - src/live/messages_ru.py
  - src/answer/russian_qna.py
  - tests/conftest.py
  - tests/test_live_enrichment.py
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
must_haves:
  truths:
    - "After retrieval, empty evidence + live gate → live fetch; success returns status ready, details.code OK, details.live with provider f1api.dev and as_of ending Z, message contains Актуально на and the same as_of substring."
    - "Live required but client raises LiveUpstreamError → status failed, details.code LIVE_UNAVAILABLE, fixed RU message literal, evidence [], no structured_answer or confidence keys."
    - "Empty evidence and gate false keeps Phase 4 RETRIEVAL_NO_EVIDENCE message and shape unchanged."
    - "Non-empty evidence and non-live-primary query never calls F1ApiClient.fetch_current_next (monkeypatch/spy)."
  artifacts:
    - path: "src/api/chat.py"
      provides: "Post-retrieval live branch ordering per D-01–D-04, D-09–D-11"
    - path: "src/main.py"
      provides: "app.state.f1_api_client default from F1ApiClient.from_env()"
    - path: "src/live/messages_ru.py"
      provides: "LIVE_UNAVAILABLE_MESSAGE_RU constant"
    - path: "tests/test_live_enrichment.py"
      provides: "FastAPI TestClient coverage for LIVE-01..03"
  key_links:
    - from: "src/api/chat.py"
      to: "src/integrations/f1api_client.py"
      via: "request.app.state.f1_api_client.fetch_current_next"
      pattern: "fetch_current_next|f1_api_client"
    - from: "src/api/chat.py"
      to: "src/live/gate.py"
      via: "requires_live_data"
      pattern: "requires_live_data"
    - from: "src/api/chat.py"
      to: "src/live/messages_ru.py"
      via: "LIVE_UNAVAILABLE_MESSAGE_RU"
      pattern: "LIVE_UNAVAILABLE_MESSAGE_RU|Актуально на"
---

<objective>
Wire `/next_message` to call the live client only after historical retrieval, only when the gate passes, and only on the empty-evidence v1 path (D-02 priority). Surface `details["live"]` and freshness in the Russian `message` on success; return `LIVE_UNAVAILABLE` degraded mode on outage (D-09–D-11). Inject `F1ApiClient` via `app.state`.

Purpose: Satisfy LIVE-01, LIVE-02, LIVE-03 end-to-end per locked CONTEXT decisions D-01–D-11, D-13.
Output: Updated FastAPI app + chat route, message constants, pytest integration tests with mocked client.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-live-enrichment-freshness/05-CONTEXT.md
@src/api/chat.py
@src/models/api_contracts.py
@src/answer/russian_qna.py
@src/main.py
@tests/conftest.py

<interfaces>
From `src/api/chat.py` (current): after `evidence = format_evidence(...)`, success branch builds `details` with `"code": "OK"`, `structured_answer`, `confidence`; empty branch uses `"code": "RETRIEVAL_NO_EVIDENCE"` and message exactly `Недостаточно исторических данных в базе f1db.`

From `src/integrations/f1api_client.py` (05-01): `F1ApiClient.from_env()`, `fetch_current_next() -> tuple[dict, str]`, `LiveUpstreamError`.

From `src/live/gate.py` (05-01): `requires_live_data(normalized_query: str, raw_user_text: str) -> bool`.

From `src/models/api_contracts.py` (05-01): `LiveDetails`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Russian copy helpers and structured live success payload builders</name>
  <read_first>
    - src/answer/russian_qna.py
    - src/models/api_contracts.py
    - .planning/phases/05-live-enrichment-freshness/05-CONTEXT.md
  </read_first>
  <files>src/live/messages_ru.py, src/answer/russian_qna.py</files>
  <action>
    1. Create `src/live/messages_ru.py` exporting **exactly** this constant (single line, pytest-locked per D-11):
       - `LIVE_UNAVAILABLE_MESSAGE_RU = "Живая база F1 сейчас недоступна; я не отвечаю по актуальным данным, чтобы не выдумывать результат."`
    2. In `src/answer/russian_qna.py` add:
       - `def live_fresh_user_message_ru(*, as_of_utc_z: str, summary_ru: str) -> str` returning a **single line** that **must** contain the substring `Актуально на ` immediately followed by the **exact** `as_of_utc_z` string passed in (D-07 single-source with D-13), then separator `" — "` then `summary_ru` (short human summary, max 160 chars truncated with `…` if longer).
       - `def build_live_structured_ru_answer(*, summary_ru: str, citation_label: str = "live:f1api.dev") -> StructuredRUAnswer` building one `AnswerSection(heading="Актуальные данные", body=summary_ru)` and `sources_block_ru` with first line `Источники:` and second line `[1] {citation_label} — {first 80 chars of summary_ru}`.
       - `def live_qna_confidence() -> QnAConfidence` returning `QnAConfidence(tier_ru="средняя", score=0.55)` (literal tier and score fixed for live path).
    3. Add helper `def summarize_live_next_payload_ru(payload: dict) -> str` in `russian_qna.py` (or colocate next to message builder) that extracts a **deterministic** short Russian summary from `payload` keys commonly present on `/api/current/next` (e.g. `raceName`, `Circuit`, nested `location.locality`) — if structure unknown, use `json.dumps(payload, ensure_ascii=False)[:200] + "…"` as last resort so tests can pass fixed fixture JSON.
  </action>
  <acceptance_criteria>
    - `rg "LIVE_UNAVAILABLE_MESSAGE_RU" src/live/messages_ru.py` matches the exact sentence above (grep the unique substring `Живая база F1`).
    - `rg "def live_fresh_user_message_ru" src/answer/russian_qna.py` matches.
    - `rg "Актуально на " src/answer/russian_qna.py` matches.
    - `rg "def build_live_structured_ru_answer" src/answer/russian_qna.py` matches.
    - `python3 -c "from src.live.messages_ru import LIVE_UNAVAILABLE_MESSAGE_RU; from src.answer.russian_qna import live_fresh_user_message_ru; ao='2026-03-27T12:00:00Z'; m=live_fresh_user_message_ru(as_of_utc_z=ao, summary_ru='тест'); assert ao in m and 'Актуально на ' in m"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -c "from src.live.messages_ru import LIVE_UNAVAILABLE_MESSAGE_RU; from src.answer.russian_qna import live_fresh_user_message_ru; ao='2026-03-27T12:00:00Z'; m=live_fresh_user_message_ru(as_of_utc_z=ao, summary_ru='тест'); assert ao in m"</automated>
  </verify>
  <done>Canonical degraded RU string and live success message formatter enforce identical as_of in user text and future details.live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject F1ApiClient and implement next_message live branches</name>
  <read_first>
    - src/api/chat.py
    - src/main.py
    - src/sessions/store.py
    - src/integrations/f1api_client.py
    - src/live/gate.py
    - src/live/messages_ru.py
    - src/answer/russian_qna.py
    - src/models/api_contracts.py
  </read_first>
  <files>src/main.py, src/api/chat.py</files>
  <action>
    **Locked routing (D-01, D-02, D-04, D-05–D-09, D-11):**
    - D-01: Order stays `resolve_entities` → `retrieve_historical_context` → `format_evidence` → **then** live logic. Never call `fetch_current_next` before evidence is computed.
    - D-02 v1: Live attempt only when `not evidence` **and** `requires_live_data(normalized_query=normalized_query, raw_user_text=_session.next_message)` is True. When `not evidence` and gate False, preserve **exact** existing abstention: `status="failed"`, `details={"code":"RETRIEVAL_NO_EVIDENCE","evidence":[]}`, `message="Недостаточно исторических данных в базе f1db."`
    - D-04: When `evidence` non-empty, keep existing Phase 4 success block **unchanged** (no live call). **Mandatory:** **no** `fetch_current_next` when `evidence` is non-empty.
    - D-05–D-08: On live success: `status="ready"`, `details["code"]="OK"`, `details["evidence"]=[]`, add `details["live"] = LiveDetails(as_of=as_of, provider="f1api.dev", endpoint_key="current_next").model_dump()`, include `structured_answer=build_live_structured_ru_answer(...).model_dump()`, `confidence=live_qna_confidence().model_dump()`, retain `normalized_query` and `canonical_entity_ids` keys like the historical success branch.
    - D-07: `message = live_fresh_user_message_ru(as_of_utc_z=as_of, summary_ru=summarized)`.
    - D-09–D-11: On `LiveUpstreamError` from `fetch_current_next` when live branch taken: `status="failed"`, `store.set_status(..., "failed", "LIVE_UNAVAILABLE")`, `details = {"code": "LIVE_UNAVAILABLE", "evidence": []}` **without** `structured_answer`, **without** `confidence`, `message=LIVE_UNAVAILABLE_MESSAGE_RU` exactly.
    - **Forbidden:** `getattr(request.app.state, "f1_api_client", None) or F1ApiClient.from_env()`. **Mandatory:** `main.py` sets `app.state.f1_api_client`; `chat.py` uses `request.app.state.f1_api_client` only.

    **Steps:**
    1. In `src/main.py` after `app` creation, set `app.state.f1_api_client = F1ApiClient.from_env()` (import from `src.integrations.f1api_client`).
    2. Refactor `next_message` in `src/api/chat.py`:
       - Import `requires_live_data`, `LiveDetails`, `F1ApiClient`, `LiveUpstreamError`, `LIVE_UNAVAILABLE_MESSAGE_RU`, and new helpers from `russian_qna`.
       - After building `evidence` list, if **non-empty**: use **existing** structured historical path (unchanged).
       - If **empty**: if `requires_live_data(...)` False → current RETRIEVAL branch.
       - If **empty** and gate True → try `request.app.state.f1_api_client.fetch_current_next()`; success builds live success `details` + `message` per behavior block; failure builds LIVE_UNAVAILABLE payload.
    3. Ensure `set_status` failure codes: `RETRIEVAL_NO_EVIDENCE` vs `LIVE_UNAVAILABLE` match the branch taken.
  </action>
  <acceptance_criteria>
    - `python3 -c "import pathlib; t=pathlib.Path('src/api/chat.py').read_text(); assert t.count('.fetch_current_next(')==1"` exits 0 (exactly one invocation).
    - `rg "LIVE_UNAVAILABLE" src/api/chat.py` matches.
    - `rg "f1_api_client" src/main.py` matches.
    - `python3 -c "from src.main import app; assert hasattr(app.state, 'f1_api_client')"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -c "import pathlib; c=pathlib.Path('src/api/chat.py').read_text(); m=pathlib.Path('src/main.py').read_text(); assert c.count('.fetch_current_next(')==1; assert 'LIVE_UNAVAILABLE' in c; assert 'f1_api_client' in m; assert 'F1ApiClient.from_env()' not in c"</automated>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -c "from src.main import app; assert hasattr(app.state, 'f1_api_client')"</automated>
  </verify>
  <done>next_message implements D-01–D-04 and D-09–D-11 with typed live details on success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FastAPI tests and conftest client reset</name>
  <read_first>
    - tests/conftest.py
    - tests/test_api_async_contracts.py
    - src/api/chat.py
    - src/integrations/f1api_client.py
  </read_first>
  <files>tests/conftest.py, tests/test_live_enrichment.py</files>
  <action>
    1. Update `tests/conftest.py` `reset_state` fixture to assign a **fresh** `app.state.f1_api_client = F1ApiClient.from_env()` each test (or a lightweight stub implementing `fetch_current_next` that raises by default — **mandatory:** use real `F1ApiClient` with `httpx.MockTransport` injected by replacing `client._client` in tests **or** attach a `DummyClient` with `fetch_current_next` monkeypatched on `app.state`).
    2. Add `tests/test_live_enrichment.py` with `TestClient` tests:
       - `test_live_enriches_when_rag_empty_and_gate_true`: monkeypatch `resolve_entities`, `retrieve_historical_context` to empty hits, set `session.next_message` to `"current standings please"`, replace `app.state.f1_api_client` with an object whose `fetch_current_next` returns `({"raceName": "Test GP"}, "2026-03-27T12:00:00Z")`; assert `status=="ready"`, `details["code"]=="OK"`, `"live" in details`, `details["live"]["provider"]=="f1api.dev"`, `details["live"]["as_of"]=="2026-03-27T12:00:00Z"`, `details["live"]["as_of"] in payload["message"]`, `Актуально на ` in `payload["message"]`, `structured_answer` in details, `confidence` in details.
       - `test_retrieval_no_evidence_when_gate_false`: empty retrieval, `session.next_message="who won monaco 1996"`, dummy client raises if called; assert `RETRIEVAL_NO_EVIDENCE` and historical abstention message unchanged.
       - `test_live_unavailable_degraded`: gate true, `fetch_current_next` raises `LiveUpstreamError("boom")`; assert `status=="failed"`, `details["code"]=="LIVE_UNAVAILABLE"`, `payload["message"]==LIVE_UNAVAILABLE_MESSAGE_RU` (import constant), `"structured_answer" not in details`, `"confidence" not in details`, `details["evidence"]==[]`.
       - `test_no_live_when_evidence_present`: monkeypatch non-empty evidence; dummy client that sets `called=False` / raises if called; assert historical success path and client not called.
  </action>
  <acceptance_criteria>
    - `rg "test_live_enriches_when_rag_empty" tests/test_live_enrichment.py` matches.
    - `rg "LIVE_UNAVAILABLE_MESSAGE_RU" tests/test_live_enrichment.py` matches.
    - `python3 -m pytest tests/test_live_enrichment.py -x` exits 0.
    - `python3 -m pytest tests/ -x` exits 0 (full suite green).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexshrestha/Documents/pet/AIAgent && python3 -m pytest tests/test_live_enrichment.py tests/test_qna_reliability.py tests/test_api_async_contracts.py -x</automated>
  </verify>
  <done>Integration tests lock LIVE-01..03 behaviors without outbound network.</done>
</task>

</tasks>

<verification>
Run full `python3 -m pytest` from repo root; spot-check `rg "fetch_current_next" src/api/chat.py -n`.
</verification>

<success_criteria>
/next_message satisfies LIVE-01, LIVE-02, LIVE-03 with deterministic gate, live details on success, and explicit outage messaging without structured_answer leakage on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-enrichment-freshness/05-02-SUMMARY.md`
</output>
