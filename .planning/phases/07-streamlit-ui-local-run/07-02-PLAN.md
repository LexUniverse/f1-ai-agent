---
phase: 07-streamlit-ui-local-run
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - api.py
  - README.md
  - .env.example
autonomous: true
requirements:
  - RUN-01
must_haves:
  truths:
    - "README.md contains a **Local run (v1.1)** section documenting: (1) install deps, (2) start API via **`python api.py`** **or** **`uvicorn src.main:app --reload --host 127.0.0.1 --port 8000`**, (3) start UI via **`streamlit run streamlit_app.py`**, (4) Docker not required."
    - "Root **`api.py`** exists and invokes uvicorn with **`src.main:app`** bound to **127.0.0.1:8000** (reload optional; default dev-friendly)."
    - "**.env.example** documents **`F1_API_BASE_URL=http://127.0.0.1:8000`** (comment line) per D-01."
  artifacts:
    - path: "api.py"
      provides: "uvicorn entry for RUN-01"
    - path: "README.md"
      provides: "Local run section with both commands"
    - path: ".env.example"
      provides: "F1_API_BASE_URL comment"
  key_links:
    - from: "README.md"
      to: "api.py"
      via: "documented launch"
      pattern: "python api.py|streamlit run"
---

<objective>
Complete **RUN-01**: document and ship a **root `api.py`** uvicorn shim, extend **`.env.example`** with **`F1_API_BASE_URL`**, and add a **README** local-run section for API + Streamlit without Docker.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-streamlit-ui-local-run/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/main.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Root api.py uvicorn shim</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/main.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/07-streamlit-ui-local-run/07-CONTEXT.md
  </read_first>
  <action>
    1. Create **`/Users/alexshrestha/Documents/pet/AIAgent/api.py`** at repository root with:
       ```python
       import uvicorn

       if __name__ == "__main__":
           uvicorn.run("src.main:app", host="127.0.0.1", port=8000, reload=True)
       ```
       (Exact string **`src.main:app`** required for RUN-01 grep stability.)
    2. If **`uvicorn`** is not in **`requirements.txt`**, add **`uvicorn[standard]`** with a bounded pin consistent with FastAPI (e.g. **`uvicorn[standard]>=0.27,<0.30`** or match project if already constrained elsewhere).
  </action>
  <acceptance_criteria>
    - `rg 'uvicorn.run\\(\"src.main:app\"' /Users/alexshrestha/Documents/pet/AIAgent/api.py` exits 0.
    - `python3 -c "import runpy; runpy.run_path('api.py', run_name='__noop__')" 2>/dev/null || true` — file must be valid Python; alternatively `python3 -m py_compile api.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: README local run + .env.example F1_API_BASE_URL</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/README.md
    - /Users/alexshrestha/Documents/pet/AIAgent/.env.example
  </read_first>
  <action>
    1. **`README.md`**: Add section heading **`## Local run (v1.1)`** containing literal substrings **`python api.py`**, **`uvicorn src.main:app`**, **`streamlit run streamlit_app.py`**, and stating Docker is **not** required for v1.1.
    2. **`.env.example`**: Add commented line **`# F1_API_BASE_URL=http://127.0.0.1:8000`** (or equivalent comment `# Streamlit → API`) in the application variables area.
  </action>
  <acceptance_criteria>
    - `rg "## Local run \\(v1\\.1\\)" /Users/alexshrestha/Documents/pet/AIAgent/README.md` exits 0.
    - `rg "python api\\.py" /Users/alexshrestha/Documents/pet/AIAgent/README.md` exits 0.
    - `rg "streamlit run streamlit_app\\.py" /Users/alexshrestha/Documents/pet/AIAgent/README.md` exits 0.
    - `rg "F1_API_BASE_URL" /Users/alexshrestha/Documents/pet/AIAgent/.env.example` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- If **`README.md`** does not exist at repo root, create it with minimal title + **`## Local run (v1.1)`** section only (no unrelated boilerplate).
