---
phase: 07-streamlit-ui-local-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/api_contracts.py
  - src/api/chat.py
  - src/ui/f1_chat_http.py
  - streamlit_app.py
  - requirements.txt
  - tests/test_api_async_contracts.py
  - tests/test_streamlit_client.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "`StartChatRequest` includes optional field `question: str | None = None`; when provided and non-empty after strip, `start_chat` assigns `session.next_message` on the newly created session (UI-01)."
    - "`streamlit_app.py` calls `POST {base}/start_chat` with JSON `access_code` and `question`, stores UUID `session_id`, polls `GET {base}/message_status` with header `X-Session-Id`, then `POST {base}/next_message` once after terminal status (UI-02)."
    - "Assistant turn renders `message`, `details.confidence` (tier_ru/score), `details.structured_answer.sources_block_ru` when present, `details.live` and `details.synthesis` per D-03–D-06 (UI-03)."
    - "`requirements.txt` pins `streamlit` and ensures `httpx` is pinned if not already listed."
  artifacts:
    - path: "src/ui/f1_chat_http.py"
      provides: "sync helpers using httpx against /start_chat, /message_status, /next_message"
    - path: "streamlit_app.py"
      provides: "operator UI, session_state history, Новый чат, polling 0.5–1s, timeout 30–60s"
    - path: "tests/test_streamlit_client.py"
      provides: "MockTransport coverage for HTTP client"
  key_links:
    - from: "streamlit_app.py"
      to: "src/ui/f1_chat_http.py"
      via: "import"
      pattern: "f1_chat_http|F1ChatClient"
---

<objective>
Deliver **UI-01–UI-03**: minimal API contract extension so `/start_chat` accepts the operator **question**, plus a **httpx** client module and **Streamlit** app that poll/fetch the async lifecycle and surface **message**, **confidence**, **citations**, **live**, and **synthesis** per `07-CONTEXT.md` and `07-UI-SPEC.md`.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-streamlit-ui-local-run/07-CONTEXT.md
@.planning/phases/07-streamlit-ui-local-run/07-RESEARCH.md
@.planning/phases/07-streamlit-ui-local-run/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
@src/models/api_contracts.py
@src/api/chat.py
@tests/conftest.py
@tests/test_api_async_contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StartChatRequest.question and start_chat wiring</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_api_async_contracts.py
  </read_first>
  <action>
    1. In `src/models/api_contracts.py`, add to **`StartChatRequest`**: **`question: str | None = None`** (field after `access_code`, default `None`).
    2. In `src/api/chat.py` **`start_chat`**, after **`session = session_store.create(...)`** and before return: if **`payload.question` is not None**, set **`session.next_message = payload.question.strip()`** (if strip yields empty string, set **`session.next_message = payload.question`** only when non-empty after strip — **mandatory:** if stripped empty, leave default **`next_message`** unchanged from `Session` dataclass default **or** set to **`"response"`** explicitly to match tests).
    3. Add test **`test_start_chat_optional_question_sets_session_next_message`** in **`tests/test_api_async_contracts.py`**: `POST /start_chat` with **`{"access_code": "ABC123", "question": "  Макс Ферстаппен  "}`**; load session from **`app.state.session_store.get(session_id)`**; assert **`session.next_message == "Макс Ферстаппен"`** (strip behavior).
  </action>
  <acceptance_criteria>
    - `rg "question: str \\| None" src/models/api_contracts.py` exits 0.
    - `next_message = payload.question.strip()` appears in `src/api/chat.py` inside a guard that only runs when `payload.question is not None` — verify with `rg "payload.question" src/api/chat.py` exits 0.
    - `pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_api_async_contracts.py -k "start_chat_optional_question" -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: httpx client module src/ui/f1_chat_http.py</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/07-streamlit-ui-local-run/07-RESEARCH.md
  </read_first>
  <action>
    1. Create package marker **`src/ui/__init__.py`** (empty) if missing.
    2. Create **`src/ui/f1_chat_http.py`** with functions (names exact for tests): **`start_chat_http(client: httpx.Client, base_url: str, access_code: str, question: str) -> str`** returning **`session_id` string** from JSON **`session_id`**; **`get_message_status(client, base_url, session_id) -> dict`** parsing JSON from **`GET /message_status`** with header **`X-Session-Id`**; **`post_next_message(client, base_url, session_id) -> dict`** for **`POST /next_message`** with same header and **`json={}`**.
    3. Use **`base_url.rstrip("/")`** for joins; timeout: **`httpx.Timeout(30.0)`** on client or per-request — document in module docstring.
    4. On HTTP error responses, raise **`httpx.HTTPStatusError`** or wrap with clear exception carrying response text — Streamlit layer will surface.
  </action>
  <acceptance_criteria>
    - `test -f /Users/alexshrestha/Documents/pet/AIAgent/src/ui/f1_chat_http.py` exits 0.
    - `rg "def start_chat_http" src/ui/f1_chat_http.py` exits 0.
    - `rg "X-Session-Id" src/ui/f1_chat_http.py` exits 0 with at least 2 matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_streamlit_client.py and streamlit_app.py + deps</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/ui/f1_chat_http.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/07-streamlit-ui-local-run/07-UI-SPEC.md
  </read_first>
  <action>
    1. **`requirements.txt`**: append **`streamlit>=1.33.0,<2`** (or pin exact **`streamlit==1.42.0`** if you prefer single version); add **`httpx==0.28.1`** if **`httpx`** is not already a line in **`requirements.txt`**.
    2. Add **`tests/test_streamlit_client.py`**: use **`httpx.MockTransport`** to assert **`start_chat_http`** sends **`POST .../start_chat`** with JSON body containing **`access_code`** and **`question`**; mock sequence for status then next_message optional.
    3. Create root **`streamlit_app.py`**: read **`os.environ.get("F1_API_BASE_URL", "http://127.0.0.1:8000")`**; **`st.session_state`** keys for **`session_id`**, **`messages`** list of `{role, content}` or similar; primary CTA label **`Отправить вопрос`**; **`Новый чат`** clears state; polling loop **sleep 0.75** between status polls, break on **`ready|failed`**, max wall clock **60 s**; display **`message`**, confidence block, **`sources_block_ru`**, expanders for **Live** / **synthesis** per CONTEXT.
  </action>
  <acceptance_criteria>
    - `rg "^streamlit" /Users/alexshrestha/Documents/pet/AIAgent/requirements.txt` exits 0.
    - `pytest /Users/alexshrestha/Documents/pet/AIAgent/tests/test_streamlit_client.py -x` exits 0.
    - `rg "Отправить вопрос" /Users/alexshrestha/Documents/pet/AIAgent/streamlit_app.py` exits 0.
    - `rg "F1_API_BASE_URL" /Users/alexshrestha/Documents/pet/AIAgent/streamlit_app.py` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- Preserve all existing tests: optional **`question`** means **`POST {"access_code":"ABC123"}`** still valid.
- Do not add Docker or change GigaChat/RAG logic in this plan.
