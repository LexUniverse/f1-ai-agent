---
phase: 02-async-backend-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_api_async_contracts.py
  - tests/conftest.py
  - src/models/api_contracts.py
  - src/auth/errors.py
  - src/main.py
  - src/api/chat.py
autonomous: true
requirements:
  - API-01
  - API-04
must_haves:
  truths:
    - "Client can start chat asynchronously and receive a UUIDv4 session handle."
    - "Invalid inputs are rejected with a unified error envelope."
    - "Auth and validation failures return machine-readable error codes in one shape."
  artifacts:
    - path: "src/models/api_contracts.py"
      provides: "Typed request/response and error-envelope contracts."
    - path: "src/main.py"
      provides: "Global exception handlers that normalize API errors."
    - path: "tests/test_api_async_contracts.py"
      provides: "Contract tests for start endpoint and validation envelope."
  key_links:
    - from: "src/api/chat.py"
      to: "src/models/api_contracts.py"
      via: "FastAPI request/response model binding"
      pattern: "response_model=.*StartChatResponse"
    - from: "src/main.py"
      to: "src/auth/errors.py"
      via: "HTTPException detail to unified error envelope translation"
      pattern: "exception_handler"
---

<objective>
Establish contract-first async API foundations for Phase 02 by introducing typed shared schemas, unified error envelopes, and a tested `/start_chat` bootstrap flow.

Purpose: Lock stable API boundaries early so downstream async status and next-message behavior can build on deterministic contracts.
Output: Shared Pydantic contracts, global error normalization handlers, and passing contract tests for API-01/API-04.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-backend-contracts/02-CONTEXT.md
@.planning/phases/02-async-backend-contracts/02-RESEARCH.md
@src/api/chat.py
@src/main.py
@src/auth/errors.py
@tests/test_auth.py

<interfaces>
From `src/api/chat.py`:
- `StartChatRequest(access_code: str | None)`
- `StartChatResponse(session_id: str)`
- Endpoints: `POST /start_chat`, `GET /message_status`, `POST /next_message`

From `src/auth/errors.py`:
- `unauthorized_error(code, message, retry_after_seconds=None) -> HTTPException`
- Existing machine codes: `AUTH_INVALID_CODE`, `AUTH_MISSING_CODE`, `AUTH_UNAUTHORIZED`, `AUTH_COOLDOWN`

From `src/sessions/store.py`:
- `SessionStore.create(authorized: bool) -> Session` (currently UUID-backed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave-0 async contract test scaffold</name>
  <read_first>
    - tests/test_auth.py
    - tests/conftest.py
    - .planning/phases/02-async-backend-contracts/02-VALIDATION.md
  </read_first>
  <files>tests/test_api_async_contracts.py, tests/conftest.py</files>
  <behavior>
    - Test 1: `/start_chat` with valid code returns HTTP 200 and `session_id` that parses as UUIDv4 (per D-01, D-03).
    - Test 2: Invalid payload to async endpoints returns unified `{error:{code,message,details}}` envelope (per D-07, D-09).
    - Test 3: Auth failures also conform to the same envelope shape with stable machine codes (per D-08).
  </behavior>
  <action>Author `tests/test_api_async_contracts.py` with explicit tests for API-01 and API-04 first (failing before implementation). Add a reusable envelope assertion helper either in `tests/conftest.py` or local test utility to avoid drift across all new tests.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py::test_start_chat_returns_uuid_session_id -x</automated>
  </verify>
  <acceptance_criteria>Test file exists, tests execute, and at least one newly added assertion fails before implementation to satisfy RED phase of TDD.</acceptance_criteria>
  <done>Wave-0 contract tests are present and target UUIDv4 plus unified envelope behavior with machine-readable code checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce shared contract models and global error normalization</name>
  <read_first>
    - src/main.py
    - src/auth/errors.py
    - src/models/auth.py
  </read_first>
  <files>src/models/api_contracts.py, src/auth/errors.py, src/main.py</files>
  <behavior>
    - Validation and auth errors produce identical top-level envelope shape (per D-07, D-08, D-09).
    - Envelope includes machine code and structured details object for downstream clients.
  </behavior>
  <action>Create `src/models/api_contracts.py` for shared request/response/error schemas (including error envelope model). Update auth error raising/utilities so existing auth machine codes are preserved but emitted in unified envelope form. Add app-level exception handlers in `src/main.py` for `HTTPException` and `RequestValidationError` to normalize all failures into `{ "error": { "code": "...", "message": "...", "details": {...} } }` per D-07..D-09.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py::test_invalid_payload_returns_enveloped_validation_error -x</automated>
  </verify>
  <acceptance_criteria>Both validation and auth failures return `error.code`, `error.message`, and `error.details` fields with no legacy top-level `detail` payload in the new tests.</acceptance_criteria>
  <done>Global error normalization is centralized and consistent across endpoint validation and auth-domain failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock `/start_chat` bootstrap contract to API-01</name>
  <read_first>
    - src/api/chat.py
    - src/sessions/store.py
    - .planning/phases/02-async-backend-contracts/02-CONTEXT.md
  </read_first>
  <files>src/api/chat.py, src/models/api_contracts.py, tests/test_api_async_contracts.py</files>
  <behavior>
    - `/start_chat` is the single session bootstrap and returns handle only after successful auth (per D-03).
    - Returned `session_id` is UUIDv4 string format (per D-01).
  </behavior>
  <action>Refactor `POST /start_chat` to use shared request/response models from `src/models/api_contracts.py`, keeping auth-first sequencing. Ensure session creation happens only on successful authorization and response contract stays strict. Keep backwards-compatible machine codes while producing enveloped failures.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py -x</automated>
  </verify>
  <acceptance_criteria>Start endpoint reliably returns typed response with UUIDv4 session handle and fails via unified envelope for invalid/unauthorized requests.</acceptance_criteria>
  <done>API-01 and API-04 contract behavior for bootstrap flow is fully covered and passing in dedicated async contract tests.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_api_async_contracts.py -x`
- `pytest tests/test_auth.py -x`
</verification>

<success_criteria>
- Start-chat contract is typed and deterministic.
- Unified error envelope is globally enforced for auth and validation paths.
- Dedicated contract tests for API-01/API-04 are green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-async-backend-contracts/02-01-SUMMARY.md`
</output>
