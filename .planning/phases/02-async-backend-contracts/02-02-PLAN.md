---
phase: 02-async-backend-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/models/api_contracts.py
  - src/sessions/store.py
  - src/auth/dependencies.py
  - src/api/chat.py
  - tests/test_api_async_contracts.py
autonomous: true
requirements:
  - API-02
  - API-03
  - API-04
must_haves:
  truths:
    - "Client can poll async message status and receive structured lifecycle state."
    - "Client can fetch next assistant message with deterministic contract behavior."
    - "Session errors map distinctly to 404, 401, and 410 and remain envelope-normalized."
  artifacts:
    - path: "src/models/api_contracts.py"
      provides: "Status and next-message response contracts with constrained state values."
    - path: "src/sessions/store.py"
      provides: "Session lifecycle checks including expiration semantics."
    - path: "tests/test_api_async_contracts.py"
      provides: "API-02/API-03 polling and response-flow contract assertions."
  key_links:
    - from: "src/api/chat.py"
      to: "src/auth/dependencies.py"
      via: "session dependency guard"
      pattern: "Depends\\(_session_dependency\\)"
    - from: "src/auth/dependencies.py"
      to: "src/sessions/store.py"
      via: "session existence/authorization/expiry mapping"
      pattern: "404|401|410"
---

<objective>
Implement deterministic async polling and next-message contracts on top of Plan 01 foundations, including strict status lifecycle and session-state HTTP mapping.

Purpose: Complete client-visible async flow with contract stability and predictable failure semantics.
Output: Typed status and next-message endpoints that satisfy API-02/API-03 with envelope-safe error behavior.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-async-backend-contracts/02-CONTEXT.md
@.planning/phases/02-async-backend-contracts/02-RESEARCH.md
@.planning/phases/02-async-backend-contracts/02-01-PLAN.md
@src/api/chat.py
@src/auth/dependencies.py
@src/sessions/store.py
@tests/test_api_async_contracts.py

<interfaces>
From Plan 01 contracts (`src/models/api_contracts.py`):
- Unified error envelope model and shared API schema types
- Start-chat request/response contracts already wired

New contracts required in this plan:
- Message status enum/literal: `queued | processing | ready | failed` (per D-04)
- Failed-status payload includes machine-readable code in envelope/details (per D-06)
- Next-message response model must be explicit and typed for client orchestration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode session lifecycle + dependency status mapping</name>
  <read_first>
    - src/auth/dependencies.py
    - src/sessions/store.py
    - .planning/phases/02-async-backend-contracts/02-CONTEXT.md
  </read_first>
  <files>src/sessions/store.py, src/auth/dependencies.py, tests/test_api_async_contracts.py</files>
  <behavior>
    - Missing/nonexistent session returns 404 (per D-02).
    - Existing but unauthorized session returns 401 (per D-02).
    - Expired session returns 410 with unified envelope (per D-02, D-07).
  </behavior>
  <action>Introduce explicit session lifecycle metadata in `SessionStore` (choose deterministic TTL policy per discretion and document in code). Update `require_authorized_session` to branch and raise correct status code for nonexistent, unauthorized, and expired sessions per D-02 while preserving machine-readable envelope contract from Plan 01.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py::test_session_state_http_mapping -x</automated>
  </verify>
  <acceptance_criteria>All three session-state branches are distinguishable by HTTP code and envelope code in automated tests.</acceptance_criteria>
  <done>Session dependency behavior is deterministic, policy-backed, and compliant with locked HTTP mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement polling-safe message status state machine</name>
  <read_first>
    - src/api/chat.py
    - src/models/api_contracts.py
    - tests/test_api_async_contracts.py
  </read_first>
  <files>src/models/api_contracts.py, src/api/chat.py, tests/test_api_async_contracts.py</files>
  <behavior>
    - Status response is constrained to `queued|processing|ready|failed` (per D-04).
    - Repeated `/message_status` polling does not mutate business outcome (per D-05).
    - `failed` status includes machine-readable failure code (per D-06).
  </behavior>
  <action>Add typed status models and endpoint response contract for `/message_status`. Implement read-only status retrieval path separate from processing side-effects so polling is safe per D-05. Ensure failed state can carry machine-readable code in error/details envelope-compatible shape.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py::test_message_status_returns_structured_state -x</automated>
  </verify>
  <acceptance_criteria>Status endpoint returns only allowed lifecycle values and repeated polls keep stable state semantics unless upstream state changes.</acceptance_criteria>
  <done>API-02 is satisfied with typed, deterministic, polling-safe status behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize `/next_message` async response contract</name>
  <read_first>
    - src/api/chat.py
    - src/models/api_contracts.py
    - tests/test_api_async_contracts.py
  </read_first>
  <files>src/models/api_contracts.py, src/api/chat.py, tests/test_api_async_contracts.py</files>
  <behavior>
    - `/next_message` returns typed response in expected async sequence (per API-03).
    - Unauthorized/invalid session paths reuse session mapping and envelope from prior tasks.
  </behavior>
  <action>Define explicit Pydantic model for next-message contract (including fields needed for deterministic client flow). Update endpoint implementation to return that schema and align behavior with status lifecycle assumptions. Extend tests for normal flow and error flow to ensure API-03 and API-04 coverage remains complete.</action>
  <verify>
    <automated>pytest tests/test_api_async_contracts.py::test_next_message_contract_flow -x</automated>
  </verify>
  <acceptance_criteria>Client can call `/next_message` after polling and receive a schema-valid payload; mismatch or invalid session paths return normalized envelope errors.</acceptance_criteria>
  <done>API-03 contract is stable, typed, and integrated with session and error semantics.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_api_async_contracts.py -x`
- `pytest tests/test_auth.py -x`
- `pytest -x`
</verification>

<success_criteria>
- Polling endpoint exposes strict lifecycle states without side effects.
- Next-message endpoint returns deterministic typed payload for async clients.
- Session and validation failures are uniformly envelope-normalized with clear HTTP differentiation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-async-backend-contracts/02-02-SUMMARY.md`
</output>
