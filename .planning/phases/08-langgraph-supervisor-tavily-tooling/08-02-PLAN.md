---
phase: 08-langgraph-supervisor-tavily-tooling
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/api/chat.py
  - src/main.py
  - src/answer/gigachat_rag.py
  - src/graph/f1_turn_graph.py
  - src/graph/tavily_tool.py
  - src/search/messages_ru.py
  - src/integrations/f1api_client.py
  - src/integrations/__init__.py
  - tests/conftest.py
  - tests/test_live_enrichment.py
  - tests/test_f1api_client.py
  - tests/test_tavily_turn.py
  - tests/test_qna_reliability.py
  - tests/test_api_async_contracts.py
  - streamlit_app.py
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - SRCH-01
  - SRCH-02
must_haves:
  truths:
    - "`rg \"f1_api_client|F1ApiClient\" src/` exits 1 (no matches under src/)."
    - "`/next_message` is `async def` and awaits `asyncio.to_thread(run_f1_turn_sync, ...)` per D-08."
    - "Tavily insufficient/failure path returns `status==\"failed\"`, `details[\"code\"]==\"WEB_SEARCH_UNAVAILABLE\"`, `evidence==[]`, and `message` equals module constant `WEB_SEARCH_UNAVAILABLE_MESSAGE_RU` from `src/search/messages_ru.py` (pytest-stable)."
    - "Successful web-augmented answers include URLs inside `structured_answer.sources_block_ru` (SRCH-01 / D-05)."
    - "`gigachat_synthesize_f1api_context` and `GIGACHAT_F1API_CONTEXT_ROUTE` removed from `gigachat_rag.py`; f1api JSON system prompt removed."
  artifacts:
    - path: "src/search/messages_ru.py"
      provides: "WEB_SEARCH_UNAVAILABLE_MESSAGE_RU"
    - path: "src/api/chat.py"
      provides: "async next_message + graph integration only"
    - path: "src/graph/f1_turn_graph.py"
      provides: "real GigaChat synthesis nodes replacing stubs"
  key_links:
    - from: "src/api/chat.py"
      to: "src/graph/f1_turn_graph.py"
      via: "run_f1_turn_sync"
      pattern: "run_f1_turn_sync|asyncio.to_thread"
---

<objective>
**Wave 2:** Wire the compiled graph into **`/next_message`** (AGT-01 completion), implement **GigaChat** query authoring + **web synthesis** with **URL citations** (SRCH-01), enforce **single Tavily** + **`WEB_SEARCH_UNAVAILABLE`** degradation (AGT-02 / D-03 / D-04), and **delete the f1api client and all `/next_message` f1api/live branches** (SRCH-02 / D-06). Update **tests** and **Streamlit** dead UI for removed `f1api` payload.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-langgraph-supervisor-tavily-tooling/08-CONTEXT.md
@.planning/phases/08-langgraph-supervisor-tavily-tooling/08-RESEARCH.md
@.planning/phases/08-langgraph-supervisor-tavily-tooling/08-01-PLAN.md
@src/api/chat.py
@src/answer/gigachat_rag.py
@src/main.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Web search messages + GigaChat web path + remove f1api synthesis</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/answer/gigachat_rag.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/models/api_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/08-langgraph-supervisor-tavily-tooling/08-CONTEXT.md
  </read_first>
  <action>
    1. Create **`src/search/messages_ru.py`** with exactly one exported constant **`WEB_SEARCH_UNAVAILABLE_MESSAGE_RU`** — a **fixed Russian** sentence stating that **web search** is temporarily unavailable and the assistant **cannot** answer the current-data part (wording **Claude’s discretion** but **immutable** after first green pytest).
    2. In **`src/answer/gigachat_rag.py`**:
       - Add **`GIGACHAT_WEB_ROUTE = "gigachat_web"`** (string used in **`details["synthesis"]["route"]`** on success).
       - Add **`gigachat_author_tavily_query(*, user_question: str) -> str`**: one **GigaChat** `chat` completion returning a **single line** search query (no JSON); on failure **return** trimmed **`user_question[:200]`** as fallback.
       - Add **`gigachat_synthesize_from_web_results(*, user_question: str, web_results: list[dict]) -> GigachatRUSynthesisResult`**: user prompt lists each result as **`[i] URL: …`** + excerpt; system prompt requires **JSON** `message` + `sections` like historical path; build **`sources_block_ru`** as **`Источники:`** lines **`[n] {url} — {truncated excerpt}`** for **every** URL in **`web_results`** (SRCH-01); set **`confidence = live_qna_confidence()`** (reuse Phase 5 live-tier pattern for external data).
       - **Remove** **`gigachat_synthesize_f1api_context`**, **`GIGACHAT_F1API_CONTEXT_ROUTE`**, **`_SYSTEM_F1API`**.
       - **Remove** **`gigachat_synthesize_live`**, **`_SYSTEM_LIVE`** only if **no** remaining imports reference them after **`chat.py`** rewrite — if still needed for template helpers, keep function but **orphan** is OK; prefer **delete** if unused.
    3. Add **`gigachat_judge_rag_sufficient(*, user_question: str, evidence: list[EvidenceItem]) -> bool`**: GigaChat returns JSON **`{\"sufficient\": true|false}`**; on parse failure return **`False`** (conservative → Tavily).
    4. Update **`src/graph/f1_turn_graph.py`** to replace **stub** synthesis nodes with calls to **`gigachat_synthesize_historical`**, **`gigachat_synthesize_from_web_results`**, and **`gigachat_author_tavily_query`** inside the **tavily** node (author query **before** `run_tavily_search_once`). Apply **template fallback** (`GIGACHAT_FALLBACK_ROUTE`, **`append_fallback_disclosure_ru`**) on **`Exception`** from GigaChat the same way **`chat.py`** did for historical path (mirror **`06-02`** behavior).
  </action>
  <acceptance_criteria>
    - `rg "WEB_SEARCH_UNAVAILABLE_MESSAGE_RU" src/search/messages_ru.py` exits 0.
    - `rg "gigachat_synthesize_f1api_context" src/answer/gigachat_rag.py` exits 1.
    - `rg "GIGACHAT_F1API_CONTEXT_ROUTE" src/answer/gigachat_rag.py` exits 1.
    - `rg "def gigachat_synthesize_from_web_results" src/answer/gigachat_rag.py` exits 0.
    - `rg "def gigachat_author_tavily_query" src/answer/gigachat_rag.py` exits 0.
    - `rg "def gigachat_judge_rag_sufficient" src/answer/gigachat_rag.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Async chat + remove f1api + map graph to NextMessageResponse</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/api/chat.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/main.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/f1_turn_graph.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/search/messages_ru.py
  </read_first>
  <action>
    1. **`src/main.py`**: Remove **`from src.integrations.f1api_client import F1ApiClient`**, remove **`app.state.f1_api_client`** assignment.
    2. **`src/integrations/__init__.py`**: Remove **`F1ApiClient`**, **`LiveUpstreamError`** exports or re-export only if another module still needs them — after deletion, **`__all__`** may be empty or removed.
    3. **Delete** **`src/integrations/f1api_client.py`** entirely.
    4. **`src/api/chat.py`**:
       - Change **`def next_message`** → **`async def next_message`**.
       - After **`resolve_entities`**, build **`F1TurnState`** initial dict (all required keys).
       - **`final = await asyncio.to_thread(run_f1_turn_sync, initial_state)`** (import **`asyncio`**, **`from src.graph import run_f1_turn_sync`** or correct path from 08-01).
       - Map **`final`** to **`NextMessageResponse`**: success → **`details`** includes **`code: OK`**, **`evidence`**, **`structured_answer`**, **`confidence`**, **`synthesis.route`**, **`normalized_query`**, **`canonical_entity_ids`**; **omit** **`details.live`** and **`details.f1api`** for all new outcomes.
       - On **`WEB_SEARCH_UNAVAILABLE`**: **`store.set_status(..., "failed", "WEB_SEARCH_UNAVAILABLE")`**, return **`message=WEB_SEARCH_UNAVAILABLE_MESSAGE_RU`**, **`details={"code":"WEB_SEARCH_UNAVAILABLE","evidence":[]}`** (no **`structured_answer`**).
       - Preserve **`RETRIEVAL_NO_EVIDENCE`** behavior only if still reachable — **after Phase 8**, pure historical miss with **declined Tavily** (should not happen) — clarify: if graph never returns that code, remove branch; **default:** **graph** returns failed **`WEB_SEARCH_UNAVAILABLE`** when no evidence and web failed; when no evidence and user question needs only historical abstention, **product** may still abstain — implement **per 08-CONTEXT**: insufficient RAG → Tavily attempt first.
       - Remove imports of **`LiveUpstreamError`**, **`LiveDetails`** if unused, **`gigachat_synthesize_f1api_context`**, **`gigachat_synthesize_live`**, **`summarize_live_next_payload_ru`**, **`build_live_structured_ru_answer`**, **`live_fresh_user_message_ru`** if unused.
    5. **`streamlit_app.py`**: Remove the **`f1api_meta`** / **`st.expander("F1 API (f1api.dev)")`** block (dead after API change).
    6. **`tests/conftest.py`**: Remove **`F1ApiClient`** import and **`app.state.f1_api_client`** setup.
  </action>
  <acceptance_criteria>
    - `rg "f1_api_client|F1ApiClient" src/` exits 1.
    - `rg "async def next_message" src/api/chat.py` exits 0.
    - `rg "asyncio.to_thread" src/api/chat.py` exits 0.
    - `rg "run_f1_turn_sync" src/api/chat.py` exits 0.
    - `test ! -f src/integrations/f1api_client.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test suite migration — remove f1api tests, add Tavily/graph API tests</name>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_live_enrichment.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_f1api_client.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_qna_reliability.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_api_async_contracts.py
    - /Users/alexshrestha/Documents/pet/AIAgent/tests/test_gigachat_rag.py
  </read_first>
  <action>
    1. **Delete** **`tests/test_f1api_client.py`**.
    2. **Replace** **`tests/test_live_enrichment.py`** with **`tests/test_tavily_turn.py`** (or rewrite file in place) covering:
       - **`test_web_search_unavailable_message_stable`**: force graph/Tavily layer to emit failure; POST **`/next_message`**; assert **`payload["details"]["code"] == "WEB_SEARCH_UNAVAILABLE"`** and **`payload["message"] == WEB_SEARCH_UNAVAILABLE_MESSAGE_RU`** imported from **`src.search.messages_ru`**.
       - **`test_historical_path_still_works`**: monkeypatch **`run_f1_turn_sync`** or **`gigachat_synthesize_historical`** so existing RAG success contract holds (**`synthesis.route`** present).
    3. Update **`tests/test_qna_reliability.py`** and **`tests/test_api_async_contracts.py`**: remove **`f1_api_client`** stubs; mock **`run_f1_turn_sync`** or inner GigaChat as needed so assertions stay valid.
    4. Extend **`tests/test_gigachat_rag.py`** (or new file) with **mocked** **`gigachat_synthesize_from_web_results`** happy path asserting **`sources_block_ru`** contains **`http`** substring.
    5. Run **`python3 -m pytest`** — all green.
  </action>
  <acceptance_criteria>
    - `test ! -f tests/test_f1api_client.py` exits 0.
    - `python3 -m pytest /Users/alexshrestha/Documents/pet/AIAgent/tests -q` exits 0.
    - `rg "WEB_SEARCH_UNAVAILABLE" tests/` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Notes for executor

- **FastAPI TestClient** with **`async def`** routes: use project’s existing **`conftest.py`** pattern; if **`TestClient` fails**, switch affected tests to **`httpx.AsyncClient(app=app, base_url="http://test")`** lifecycle — document in test module.
- **README / `.env.example` Tavily docs** belong to **Phase 10 (DOC-01)** — do **not** block Phase 8 on README unless tests require env keys (use mocks).
