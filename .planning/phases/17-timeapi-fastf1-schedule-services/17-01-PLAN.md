---
phase: 17-timeapi-fastf1-schedule-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/integrations/messages_ru.py
  - src/integrations/timeapi_client.py
  - tests/test_timeapi_client.py
  - pytest.ini
autonomous: true
requirements:
  - TIME-01
must_haves:
  truths:
    - "Single GET to TimeAPI unix endpoint within TIMEAPI_TIMEOUT (default 10); no httpx retries."
    - "Success path returns authoritative unix epoch and naive UTC datetime derived from that instant."
    - "On timeout, HTTP error, network error, or bad JSON: TimeApiError raised; fixed Russian degraded string available as TIMEAPI_UNAVAILABLE_MESSAGE_RU."
    - "Tests prove one HTTP call on success path and correct error mapping + RU constant usage."
  artifacts:
    - path: "src/integrations/messages_ru.py"
      provides: "TIMEAPI_UNAVAILABLE_MESSAGE_RU"
    - path: "src/integrations/timeapi_client.py"
      provides: "TimeApiError; fetch authoritative now via httpx"
    - path: "tests/test_timeapi_client.py"
      provides: "TIME-01 unit coverage (mocked httpx)"
  key_links:
    - from: "src/integrations/timeapi_client.py"
      to: "https://timeapi.io/api/v1/time/current/unix"
      via: "httpx.Client.get"
      pattern: "time/current/unix"
---

<objective>
Implement **TIME-01**: authoritative «now» from **TimeAPI.io** using **httpx**, env **`TIMEAPI_TIMEOUT`** (default **10**), **one HTTP GET** per successful fetch (no transport-level retries), **`TimeApiError`** on failure, and fixed Russian degraded text **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`** in **`src/integrations/messages_ru.py`**. **No** silent fallback to local `datetime.now()`. **No LangGraph tools** (Phase 18).

Purpose: Establishes the single clock for Phase 18 and for **`f1_schedule_service`** (Phase 17 plan 02).

Output: `messages_ru.py`, `timeapi_client.py`, `tests/test_timeapi_client.py`, optional **`timeapi_live`** marker in `pytest.ini`.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/graph/tavily_tool.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: TIMEAPI_UNAVAILABLE_MESSAGE_RU + timeapi_client (httpx, one GET, TimeApiError)</name>
  <files>src/integrations/messages_ru.py, src/integrations/timeapi_client.py</files>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/graph/tavily_tool.py
    - /Users/alexshrestha/Documents/pet/AIAgent/requirements.txt
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
  </read_first>
  <action>
    1. Add **`src/integrations/messages_ru.py`** exporting a constant **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`**: one fixed, user-facing Russian sentence (or short paragraph) stating that the network time service is unavailable and the assistant cannot rely on an authoritative «сейчас» — **no** dynamic exception text in the constant itself.
    2. Add **`src/integrations/timeapi_client.py`** with:
       - **`TimeApiError`**: subclass of **`Exception`**; carry a machine-oriented **`reason`** (e.g. **`"timeout"`**, **`"http"`**, **`"network"`**, **`"parse"`**) and optional **`detail`** string; document that callers may surface **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`** to end users on any **`TimeApiError`** (import from **`messages_ru`** only inside docstring or helper **`degraded_message_ru()`** returning that constant — avoid circular imports if needed by keeping messages in a leaf module).
       - Read **`TIMEAPI_TIMEOUT`** via **`float(os.environ.get("TIMEAPI_TIMEOUT", "10"))`** (mirror spirit of **`TAVILY_TIMEOUT`** in `tavily_tool.py`).
       - **`TIMEAPI_BASE = "https://timeapi.io"`** and exactly **one** successful-path HTTP call: **`GET https://timeapi.io/api/v1/time/current/unix`** using **`httpx.Client(timeout=...)`** (use **`httpx.Timeout`** if you split connect/read; default float is acceptable per research).
       - Parse JSON **`{"unix_timestamp": <int>}`**; on missing key or wrong type → **`TimeApiError`** with **`reason="parse"`**.
       - Build **naive UTC** **`datetime`** from the unix instant (e.g. **`datetime.fromtimestamp(unix, tz=timezone.utc).replace(tzinfo=None)`**) for a small **Pydantic v2** model **`TimeApiNow`** (or equivalent) with **`unix_timestamp: int`** and **`utc_naive: datetime`** exported from the module.
       - Map **`httpx.TimeoutException`** → **`TimeApiError(..., reason="timeout")`**; **`httpx.HTTPStatusError`** → **`reason="http"`**; **`httpx.RequestError`** (other) → **`reason="network"`**; always **`raise ... from e`**.
       - **Do not** enable httpx retries; **do not** call **`/api/v1/time/current/utc`** on the success path (keeps research’s **single-call** test contract; UTC display comes from unix).
       - Module docstring: state endpoints used and that **one** request is intentional (no retry unless a future phase adds it).
  </action>
  <verify>
    <automated>rg "TIMEAPI_UNAVAILABLE_MESSAGE_RU" src/integrations/messages_ru.py &amp;&amp; rg "class TimeApiError" src/integrations/timeapi_client.py &amp;&amp; rg "time/current/unix" src/integrations/timeapi_client.py &amp;&amp; rg "TIMEAPI_TIMEOUT" src/integrations/timeapi_client.py</automated>
  </verify>
  <done>Constants and client modules exist; grep checks pass; ready for Task 2 tests.</done>
  <acceptance_criteria>
    - `rg "TIMEAPI_UNAVAILABLE_MESSAGE_RU" src/integrations/messages_ru.py` finds the constant definition.
    - `rg "class TimeApiError" src/integrations/timeapi_client.py` and `rg "time/current/unix" src/integrations/timeapi_client.py` both match.
    - `rg "TIMEAPI_TIMEOUT" src/integrations/timeapi_client.py` shows default **10**.
    - `pytest tests/test_timeapi_client.py -x` passes after Task 2 adds tests (or run after both tasks).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests (mocked httpx) + optional timeapi_live marker</name>
  <files>tests/test_timeapi_client.py, pytest.ini</files>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/integrations/timeapi_client.py
    - /Users/alexshrestha/Documents/pet/AIAgent/pytest.ini
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
  </read_first>
  <behavior>
    - Success: mock transport returns 200 + `{"unix_timestamp": 1700000000}` → parsed model has that unix and matching naive UTC.
    - Single call: on success, **`httpx.Client.get`** (or transport handler) invoked **exactly once** (no retry).
    - Timeout: mock raises **`httpx.TimeoutException`** → **`TimeApiError`** with **`reason=="timeout"`**; assert degraded path references **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`** (e.g. helper or string equality on exception wrapper test).
    - HTTP 5xx / 4xx: **`raise_for_status`** path → **`TimeApiError`** with **`reason=="http"`**.
    - Optional: **`@pytest.mark.timeapi_live`** test skipped unless **`RUN_TIMEAPI_SMOKE=1`**, performing one real GET (document in test docstring).
  </behavior>
  <action>
    1. Add **`tests/test_timeapi_client.py`**: use **`httpx.MockTransport`** or **`unittest.mock.patch`** on **`httpx.Client.get`** per **17-RESEARCH.md**; import the public fetch function (name as implemented, e.g. **`fetch_timeapi_now`**).
    2. Register marker **`timeapi_live`** in **`pytest.ini`** under **`markers =`** (follow existing **`integration_index`** style).
    3. Keep default CI **offline**: live test must be **skipped** without env flag.
  </action>
  <verify>
    <automated>pytest tests/test_timeapi_client.py -x</automated>
  </verify>
  <done>TIME-01 unit tests pass; `timeapi_live` marker registered; success path asserts unix parsing and single-call behavior.</done>
  <acceptance_criteria>
    - `pytest tests/test_timeapi_client.py -x` passes.
    - `rg "timeapi_live" pytest.ini` matches.
    - `rg "unix_timestamp" tests/test_timeapi_client.py` matches success-path assertion.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest tests/test_timeapi_client.py -x` green.
- No imports from **`langgraph`**, **`langchain`**, or tool registration in new integration modules.
</verification>

<success_criteria>
- **TIME-01** satisfied: bounded timeout, one GET, structured failure + **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`**, automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/17-timeapi-fastf1-schedule-services/17-01-SUMMARY.md`
</output>

## must_haves

- Calling the time service returns **current unix + naive UTC** derived from TimeAPI **`/unix`** within **`TIMEAPI_TIMEOUT`** (default 10).
- On API/network/parse failure, behavior matches plan: **`TimeApiError`** and fixed **Russian degraded message** constant — **no** undocumented local clock fallback.
- **Single HTTP attempt** on the success path (no retries); verified by unit test call count.
