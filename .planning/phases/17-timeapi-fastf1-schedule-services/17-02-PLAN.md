---
phase: 17-timeapi-fastf1-schedule-services
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - requirements.txt
  - src/integrations/f1_schedule_service.py
  - src/integrations/__init__.py
  - tests/test_f1_schedule_service.py
autonomous: true
requirements:
  - SCHED-01
must_haves:
  truths:
    - "fastf1>=3.8.0,<4 in requirements.txt; schedule uses get_event_schedule(year, include_testing=False)."
    - "Default season year from TIME-01 UTC instant; if no future session in that year, load year+1."
    - "Next event: RoundNumber > 0, non-testing; earliest Session*DateUtc (non-NaT) strictly after now selects the weekend; payload includes naming, country/circuit fields, EventDate, per-session UTC subset."
    - "Module docstrings document Ergast / pre-2018 session time caveats; structured result includes schedule_data_quality (e.g. full_2018_plus vs ergast_limited for year < 2018)."
    - "Public exports wired in integrations/__init__.py; no LangGraph tools."
  artifacts:
    - path: "src/integrations/f1_schedule_service.py"
      provides: "resolve next GP vs TimeApi now"
    - path: "src/integrations/__init__.py"
      provides: "re-exports for Phase 18"
    - path: "tests/test_f1_schedule_service.py"
      provides: "SCHED-01 deterministic tests"
  key_links:
    - from: "src/integrations/f1_schedule_service.py"
      to: "src/integrations/timeapi_client.py"
      via: "fetch authoritative now"
    - from: "src/integrations/f1_schedule_service.py"
      to: "fastf1.get_event_schedule"
      via: "pandas DataFrame rows"
---

<objective>
Implement **SCHED-01**: **`fastf1.get_event_schedule`**, filter **`RoundNumber > 0`** and **non-testing**, pick **next grand prix** whose **earliest future `Session*DateUtc`** (skip NaT) is **strictly after** the instant from **TIME-01**; **year rollover** to **year+1** when the current season has no qualifying session; add **`fastf1>=3.8.0,<4`** to **`requirements.txt`**; document **Ergast / pre-2018** caveats in **module docstrings** and expose **`schedule_data_quality`** on the result. **Wire** public exports in **`src/integrations/__init__.py`**. **No LangGraph tools**.

Purpose: Service layer for Phase 18 worker tools.

Output: `f1_schedule_service.py`, tests, dependency pin, `__all__` exports.
</objective>

<execution_context>
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/alexshrestha/Documents/pet/AIAgent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/integrations/timeapi_client.py
@src/integrations/messages_ru.py
@src/integrations/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: requirements.txt + f1_schedule_service (FastF1, TIME-01 instant, Pydantic models)</name>
  <files>requirements.txt, src/integrations/f1_schedule_service.py</files>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/integrations/timeapi_client.py
    - /Users/alexshrestha/Documents/pet/AIAgent/requirements.txt
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
  </read_first>
  <action>
    1. Append to **`requirements.txt`**: **`fastf1>=3.8.0,<4`** (comment optional: SCHED-01 / Phase 17).
    2. Add **`src/integrations/f1_schedule_service.py`**:
       - **Module docstring (required):** Explain that for **seasons before 2018** and/or **Ergast-backed** data, FastF1 may **assume** non-race session times from a **conventional weekend layout** — only **race** time is fully reliable; **2018+** default backend gives **`Session1DateUtc`…`Session5DateUtc`** as naive UTC per FastF1 docs. Cite conceptual source: FastF1 **Supported Seasons** / Event Schedule docs (no need for URLs inside code if verbose; one line “see FastF1 docs” is OK if caveats are explicit).
       - Import **`fastf1`**; use **`fastf1.get_event_schedule(year, include_testing=False)`** only (never rely on default **`include_testing=True`**).
       - Obtain **`now_utc`**: call the **public** TIME-01 function from **`timeapi_client`** (same naive-UTC convention as schedule columns). If **`TimeApiError`** propagates, **do not** catch and substitute local time.
       - **`year = now_utc.year`**. Load schedule for **`year`**. Filter rows: **`RoundNumber > 0`**, **`EventFormat != "testing"`** (defense in depth per research). Iterate in **calendar DataFrame order** (or sort by **`EventDate`** if column present and needed for stability — document choice).
       - For each row, collect all **`SessionNDateUtc`** for N in 1..5 where column exists and value **not NaT**; define **next session** for that event as **min(timestamp strictly > now_utc)** among those; if none, skip event.
       - **Select the first event** in order that has such a next session.
       - If **no** event qualifies, repeat for **`year + 1`** with a **fresh** **`get_event_schedule(year+1, include_testing=False)`**.
       - Define **`ScheduleResolutionError`** (subclass **`Exception`**) for empty schedule, malformed frame, or no upcoming event in both years; message may be English (internal); Phase 18 will map to RU if needed.
       - Return a **Pydantic v2** model (e.g. **`NextGrandPrixSchedule`**) including: **`event_name`** / **`official_name`** (from **`EventName`** / **`OfficialEventName`** or closest available), **`country`** (**`Country`**), **`location`** (**`Location`**), **`event_date`** (**`EventDate`** as appropriate type), **`sessions_utc`**: list or dict of session **label** (**`SessionN`**) → **naive UTC datetime** for only **non-null** **`SessionNDateUtc`**, **`schedule_data_quality`**: **`Literal["full_2018_plus", "ergast_limited"]`** with **`ergast_limited`** when **`year < 2018`** OR when documentation indicates ergast-only path for that call (if backend cannot be detected, use **year < 2018** rule at minimum).
       - Optional explicit **`season_year: int`** argument on the resolver: if **`None`**, derive from **`now_utc.year`** then rollover logic as above; if set, still compare session times against TIME-01 **`now`** (per REQUIREMENTS: default year from TIME-01, optional override).
    3. **Do not** add LangGraph **`@tool`** or register tools in this file.
  </action>
  <verify>
    <automated>rg "fastf1>=3.8.0,<4" requirements.txt && rg "get_event_schedule" src/integrations/f1_schedule_service.py && rg "include_testing=False" src/integrations/f1_schedule_service.py && rg "schedule_data_quality" src/integrations/f1_schedule_service.py</automated>
  </verify>
  <done>fastf1 pinned; schedule service implements selection vs TIME-01, rollover, Pydantic payload, docstring caveats.</done>
  <acceptance_criteria>
    - `rg "fastf1>=3.8.0,<4" requirements.txt` matches.
    - `rg "get_event_schedule" src/integrations/f1_schedule_service.py` matches.
    - `rg "include_testing=False" src/integrations/f1_schedule_service.py` matches.
    - `rg "ergast|2018|pre-2018|Ergast" src/integrations/f1_schedule_service.py` matches (docstring caveat coverage; case-insensitive search acceptable).
    - `rg "schedule_data_quality" src/integrations/f1_schedule_service.py` matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_f1_schedule_service.py + exports in __init__.py</name>
  <files>tests/test_f1_schedule_service.py, src/integrations/__init__.py</files>
  <read_first>
    - /Users/alexshrestha/Documents/pet/AIAgent/src/integrations/f1_schedule_service.py
    - /Users/alexshrestha/Documents/pet/AIAgent/src/integrations/__init__.py
    - /Users/alexshrestha/Documents/pet/AIAgent/.planning/phases/17-timeapi-fastf1-schedule-services/17-RESEARCH.md
  </read_first>
  <behavior>
    - **Next event:** DataFrame with two rounds; **`now`** between them → resolver picks the later round’s **earliest future session** (monkeypatch **`get_event_schedule`** to return fixed **`pandas.DataFrame`**).
    - **Filtering:** Row with **`RoundNumber==0`** or **`EventFormat=="testing"`** ignored.
    - **Rollover:** **`now`** after all sessions in year N → second **`get_event_schedule`** call for **N+1** (assert with **`monkeypatch`** call counter or side-effect list of years requested).
    - **TIME-01 coupling:** Monkeypatch **`timeapi_client`** public fetch to return a fixed **`TimeApiNow`** / frozen instant so tests are deterministic.
    - **Pre-2018:** For **`season_year=2007`** (or similar), result **`schedule_data_quality == "ergast_limited"`** (if resolver uses year for quality flag).
  </behavior>
  <action>
    1. Add **`tests/test_f1_schedule_service.py`** per **Validation Architecture** in **17-RESEARCH.md**; use **`pandas`**, **`monkeypatch.setattr`** on **`fastf1.get_event_schedule`** (target the module where it is referenced, e.g. **`src.integrations.f1_schedule_service.fastf1.get_event_schedule`**).
    2. Update **`src/integrations/__init__.py`**: set **`__all__`** to stable public names only (e.g. **`fetch_timeapi_now`** or actual name from 17-01, **`TimeApiError`**, **`TimeApiNow`**, **`TIMEAPI_UNAVAILABLE_MESSAGE_RU`**, schedule resolver function, **`NextGrandPrixSchedule`**, **`ScheduleResolutionError`** — match actual implementations). Re-export with **`from .timeapi_client import ...`**, **`from .f1_schedule_service import ...`**, **`from .messages_ru import TIMEAPI_UNAVAILABLE_MESSAGE_RU`**.
  </action>
  <verify>
    <automated>pytest tests/test_timeapi_client.py tests/test_f1_schedule_service.py -x</automated>
  </verify>
  <done>SCHED-01 tests pass; `__all__` exports public integration API for Phase 18.</done>
  <acceptance_criteria>
    - `pytest tests/test_f1_schedule_service.py -x` passes.
    - `pytest tests/test_timeapi_client.py tests/test_f1_schedule_service.py -x` passes.
    - `rg "__all__" src/integrations/__init__.py` matches and lists non-empty exports.
    - `python -c "from src.integrations import *"` or explicit imports from **`src.integrations`** succeed (adjust if package layout requires **`PYTHONPATH=.`** — document in test/README if needed; prefer same pattern as other **`tests/`** imports in repo).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest tests/test_timeapi_client.py tests/test_f1_schedule_service.py -x` green.
- `pytest` full suite green before phase sign-off.
- No LangGraph tool wiring in **`src/integrations/`**.
</verification>

<success_criteria>
- **SCHED-01** satisfied: FastF1 schedule, correct filters, next session vs TIME-01, rollover, structured payload, caveats documented, tests + exports.
</success_criteria>

<output>
After completion, create `.planning/phases/17-timeapi-fastf1-schedule-services/17-02-SUMMARY.md`
</output>

## must_haves

- Resolver returns the **next non-testing GP** with **next `Session*DateUtc` strictly after** TimeAPI-derived **now**, with **year → year+1** when nothing remains in the calendar year.
- Payload includes **event naming**, **country / circuit (Location)**, **`EventDate`**, and **session UTC times** for the upcoming weekend.
- **Ergast / pre-2018** limitations are stated in **module docstrings**; structured output carries **`schedule_data_quality`**.
- **`fastf1>=3.8.0,<4`** added; **`integrations/__init__.py`** exports the public service surface.
